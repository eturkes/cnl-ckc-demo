# audit-m1 — claim replay over M1

Target = `9020e62`. Census = 137 rows. Grade: `python3 -P .scratch/validate-report.py .scratch/agents/audit-m1.md --verdict`

Claim text per id = `.scratch/worktrees/audit-m1/audit-claims.tsv` (id, source, claim).

| id | finding | evidence |
| --- | --- | --- |
| A001 | pass: all six tools/kb/*.mjs (bag,paths,produce,build,check,reproduce) plus src/kb/manifest.ts and both tests exist at 9020e62. | `ls tools/kb/` in wt/audit-m1; tests/kb-bag.test.ts, tests/kb-live.test.ts |
| A002 | pass: package.json gate = kb:build && kb:asset-check && copy:check && contrast:check && format:check && lint && check && test && build; kb:reproduce is a separate script, absent from the chain. | `python3 -c` over package.json:scripts; gate + kb:reproduce keys |
| A003 | pass: recorded map-m1u1 17/25 and spike-m1u1-det 9/12; the validator grades both files at exactly those figures and both files survive as required. | `python3 .scratch/validate-report.py .scratch/agents/map-m1u1.md` -> 17/25; spike-m1u1-det.md -> 9/12 |
| A004 | pass: all five src/engine/*.ts (protocol,terms,session,client,worker) and tests/engine-session.test.ts exist; src/engine/budget.ts is u3s addition, not a contradiction. | `ls src/engine/ tests/engine-*.test.ts` |
| A005 | pass: vite.config.ts sets base "./", cacheDir ".vite", alias @kb -> ./kb/generated and worker.format "es", all four as recorded. | vite.config.ts:9,13,17,20 |
| A006 | pass: m1u2.md D1 records both spikes clearing 11/11 traps and rules the JS-side decoder; spike-m1u2-pl.md states 11/11 and spike-m1u2-js.md states 11 traps, and src/engine/terms.ts is the shipped JS decoder. | .agent/contracts/m1u2.md D1 preamble + D1.1; .scratch/agents/spike-m1u2-{js,pl}.md; src/engine/terms.ts |
| A007 | pass: recorded rc=0, 58 tests, 330 files 0 errors; replaying `rm -rf kb/generated && pnpm gate` at 2f78353 gave rc=0, COMPLETED 330 FILES 0 ERRORS 0 WARNINGS, 58 tests in 4 files. | /tmp/audit-historical.log 2f78353 block; historical.sh |
| A008 | pass: the built main chunk (index-*.js, 63385 B) contains zero occurrences of swipl, SWI-Prolog, wasm, Module or _PL_, while worker-*.js carries all of them and kb-<hash>.pvm emits as its own asset. | `python3` byte count over dist/assets/index-CzoEE96V.js vs worker-Dk-VoTc5.js |
| A009 | pass: both recorded NOT-verified items are filed as polish entries — "u2 browser smokes" naming the dev-server and built-output accept clause, and "u2 red suite" naming wt/test-m1u2. | .agent/polish.md:26-29 and :30-31 |
| A010 | pass: the u3 accept clause is gate-enforced — client.reset drives terminate-and-recreate and the contract is re-read as manifest.contract.documents rather than a literal, with all 171 tests green from a clean kb/generated. | tests/engine-budgets.test.ts:79, :417, :435, :482; /tmp/audit-gate.log 171 passed |
| A011 | pass: tools/kb/catalog.mjs, all four src/questions/*.ts (catalog,serialize,humanize,service) and tests/questions-live.test.ts exist. | `ls tools/kb/catalog.mjs src/questions/ tests/questions-live.test.ts` |
| A012 | pass: recorded 7/2/1/12/5/3 (memory order A/dosage/ev1/exists/B/ev3); observed the same multiset gate-enforced as [7,5,2,1,3,12] in QUESTION_IDS order. | tests/demo-lifecycle.test.ts:372-385 `expect(counts).toEqual([7, 5, 2, 1, 3, 12])`, green in my gate run |
| A013 | pass: the suite asserts each authored goal equals its exported analog under exactly one quoted-atom replacement, and P1.5 makes the four exported goals bag-verbatim by construction through the generator. | tests/questions-live.test.ts:120-132; tools/kb/catalog.mjs re-derived by kb:asset-check |
| A014 | pass: all four exported ids compare with `toBe` against `committedResult(id)` read from the bag at test time, so all four are byte-proven, not three value-equal. | tests/questions-live.test.ts:167-180; committedResult at :56-60 via `verifyBag` |
| A015 | pass: recorded byte scan in kb:asset-check with negative control rc=1; observed rc=1 "answer-oracle reach in src/demo/copy.ts" on an injected queries/answers literal, rc=0 restored. | controls.sh C4; tools/kb/check.mjs:21 PRODUCTION_ROOTS, :28 ANSWERS regex |
| A016 | pass: recorded rc=0, 339 files 0 errors, 107 tests, 3 assets; replay at 823ed4f gave rc=0, 339 FILES 0 ERRORS 0 WARNINGS, 107 tests in 6 files, "3 assets verified". | /tmp/audit-historical.log 823ed4f block |
| A017 | pass: recorded catalog hash a38e74d9f518; pnpm kb:reproduce rc=0 prints "question-catalog.json a38e74d9f518" alongside the pvm and qlf hashes. | /tmp/audit-repro-smoke.log REPRODUCE_RC=0 |
| A018 | pass: the recorded NOT-verified item is filed — .agent/polish.md carries a "u4 red suite" entry naming the diff-blind suite on wt/test-m1u4. | .agent/polish.md:61 |
| A019 | pass: src/questions/QuestionCombobox.svelte and tests/question-combobox.dom.test.ts both exist at 9020e62. | `ls src/questions/QuestionCombobox.svelte tests/question-combobox.dom.test.ts` |
| A020 | pass: vitest declares a second project named dom with environment jsdom, include tests/**/*.dom.test.ts and resolve.conditions ["browser"]; jsdom is declared ^29.1.1 and resolves to 29.x. | vite.config.ts:24,39-40; package.json devDependencies jsdom |
| A021 | pass: package.json check script is `svelte-check --tsconfig ./tsconfig.json --fail-on-warnings`, and the flag is exercised on every gate run. | package.json scripts.check; /tmp/audit-gate.log check step |
| A022 | fail(low): recorded as intake rendered in App.svelte against local $state; at 9020e62 App.svelte takes `controller?: DemoController` through $props and the selection lives in DemoController, so the claim is stale from u6 onward though it held at 5e3bf46. | src/App.svelte:13,16,21,52; .agent/contracts/m1u6.md Q1 injection seam |
| A023 | pass: the widget host is `<div role="combobox" tabindex="0">`, not a readonly input and not a button, exactly as D1 rules. | src/questions/QuestionCombobox.svelte:183-184 |
| A024 | pass: wt/spike-m1u5-lib resolves to exactly dca4f87 and neither bits-ui nor its @internationalized/date peer is installed, which is what the D2 rejection implies; the 21-case green run, 7-package count and 10.06 s gate cost are spike-local and sit in the Register. | `git rev-parse --short=7 wt/spike-m1u5-lib`; node_modules existence checks |
| A025 | pass: recorded rc=0, 342 files 0 errors 0 warnings, 133 tests, 3 assets; replay at 5e3bf46 gave rc=0, 342 FILES 0 ERRORS 0 WARNINGS, 133 tests in 7 files, 3 assets. | /tmp/audit-historical.log 5e3bf46 block |
| A026 | pass: m1u5.md carries exactly 26 predicate bullets (S1-S8, K1-K11, P1-P3, B1-B4) and a 26-row verdict table whose every verdict cell reads pass. | .agent/contracts/m1u5.md Predicates + Verdict sections; id list matches one-to-one |
| A027 | pass: recorded map-m1u5 29/29; the validator grades .scratch/agents/map-m1u5.md at 29/29 rows filled with 0 problems. | `python3 .scratch/validate-report.py .scratch/agents/map-m1u5.md` |
| A028 | pass: all four src/demo files, tools/smoke.mjs and the three suites (engine-cancel, demo-lifecycle, demo-controller.dom) exist. | `ls src/demo/ tools/smoke.mjs tests/demo-*.ts tests/engine-cancel.test.ts` |
| A029 | pass: m1u6.md D1 rules the trailing optional AbortSignal, and the diff proves the discriminator — wt/spike-m1u6-signal adds tests/spike-m1u6-signal.test.ts and modifies no existing test file. | `git diff --name-status 5e3bf46 wt/spike-m1u6-signal -- tests/` shows a single A entry |
| A030 | pass: not re-measured — the spike harnesses that produced 1.869 ms and 0.355 ms are branch-local and no shipped surface reports cancel latency. The load-bearing part holds: both figures sit far below the 62 ms granularity the contract also records, and nothing observed contradicts them. See Register. | .agent/contracts/m1u6.md D1; wt/spike-m1u6-{signal,handle} |
| A031 | pass: DEMO_BUDGET is frozen and wallClockMs is 5000; both are gate-enforced assertions, not prose. | tests/demo-lifecycle.test.ts:358-359 `Object.isFrozen(DEMO_BUDGET)` + `wallClockMs).toBe(5000)` |
| A032 | pass: recorded rc=0, 350 files 0 errors 0 warnings, 171 tests, 10 files; replay at d5017dd gave rc=0, 350 FILES 0 ERRORS 0 WARNINGS, 171 tests in 10 files. | /tmp/audit-historical.log d5017dd block |
| A033 | pass: recorded a real browser answering dosage-reduction-content against bag bytes derived through verifyBag; observed pnpm smoke rc=0 reporting exactly that with 2 rows matching the bag over 8 nested requests. | /tmp/audit-repro-smoke.log SMOKE_RC=0; tools/smoke.mjs verifyBag import |
| A034 | pass: m1u6.md carries exactly 32 predicate bullets and a 32-row verdict table, 28 plain pass plus exactly 4 marked "pass after fix" (R3, R4, V6, V10). | .agent/contracts/m1u6.md Predicates + Verdict table |
| A035 | pass: src/app.css, copy.ts, descriptor.ts, AboutPanel.svelte, copy-check.mjs, contrast.mjs and three public/licenses/*.txt all exist. | `ls src/app.css src/demo/ tools/*.mjs public/licenses/` |
| A036 | fail(low): recorded "pinned at 5.3.0 in dependencies" for all three; observed next and literata pinned exact but atkinson-hyperlegible-mono declared "^5.3.0", a caret range. Installed resolves to 5.3.0 for all three. | package.json dependencies; node_modules/@fontsource-variable/*/package.json version fields |
| A037 | pass: recorded 6 woff2 = 176732 B; observed exactly 6 woff2 in dist/assets totalling 176732 B and zero other font files in dist/. | `python3` sum over dist/assets/*.woff2 after `pnpm build` |
| A038 | pass: all three public/licenses/*.txt and their dist/licenses/ copies are byte-equal to the matching node_modules/@fontsource-variable/*/LICENSE. | `filecmp.cmp(..., shallow=False)` x3, both public/ and dist/ |
| A039 | pass: recorded 79 strings with 20/25 word ceilings; observed `copy: 79 strings pass` rc=0 and the limits declared as INSTRUCTIONS 20 / DESCRIPTIONS 25 / describe.ts 25. | /tmp/audit-gate.log copy:check; tools/copy-check.mjs:29-35 |
| A040 | pass: recorded 17 declared pairs, WCAG luminance, floored; observed `contrast: 17 pairs pass` rc=0 and `Math.floor(contrast(...) * 100) / 100`. | /tmp/audit-gate.log contrast:check; tools/contrast.mjs:112 |
| A041 | pass: recorded --border needed #8f8270 to clear 3:1; observed --border: #8f8270 in :root and the 17-pair table passing. | src/app.css:99; `pnpm contrast:check` rc=0 |
| A042 | pass: recorded rc=0, 355 files 0 errors 0 warnings, 171 tests, 3 assets; observed identical on `rm -rf kb/generated && pnpm gate` at 9020e62, 30.9 s wall. | /tmp/audit-gate.log: `COMPLETED 355 FILES 0 ERRORS 0 WARNINGS`, `171 passed`, `3 assets verified`, GATE_RC=0 |
| A043 | pass: recorded pnpm smoke rc=0; observed rc=0 answering dosage-reduction-content with 2 rows matching the bag over 8 nested requests. | /tmp/audit-repro-smoke.log SMOKE_RC=0; ephemeral port 44145 |
| A044 | pass: not re-run — the walker is tools/probe-u7.mjs on wt/map-m1u7 124e34d and needs a real browser, so the 11-state / 320-375-1280 px overflow=false result was not re-derived. Regeneration path is exact and the recorded G4-G5 fail(low) already discloses the gate gap. See Register. | .agent/contracts/m1u7.md G4-G5 and R1; wt/map-m1u7:tools/probe-u7.mjs |
| A045 | fail(low): recorded "41 type errors" and committed-only-on-wt/map-m1u7; the branch and 124e34d are right and the file is absent from the primary tree, but svelte-check over it at 9020e62 reports 64 errors, not 41. | `git show wt/map-m1u7:tools/probe-u7.mjs > tools/probe-u7.mjs && pnpm check` -> COMPLETED 356 FILES 64 ERRORS |
| A046 | pass: wt/spike-m1u1-det exists (7893c28, no SHA recorded); wt/test-m1u2 is exactly b0fb200; wt/spike-m1u2-js and wt/spike-m1u2-pl both exist. | `git rev-parse --short=7` over each branch from wt/audit-m1 |
| A047 | pass: m1u3-rev-checkset.md carries exactly 45 data rows (R01-R45); wt/test-m1u3 is exactly 22c8b97; wt/spike-m1u3-js and -pl both exist. | `python3` row count over .agent/contracts/m1u3-rev-checkset.md; `git rev-parse` |
| A048 | pass: m1u4-rev-checkset.md carries exactly 24 data rows (c01-c24); wt/test-m1u4 is exactly 0240aae; wt/spike-m1u4-gen and -src both exist. | `python3` row count over .agent/contracts/m1u4-rev-checkset.md; `git rev-parse` |
| A049 | pass: wt/spike-m1u5-lib is exactly dca4f87, wt/map-m1u7 is exactly 124e34d and wt/res-m1u7 is exactly d712fa9. | `git rev-parse --short=7 wt/spike-m1u5-lib wt/map-m1u7 wt/res-m1u7` |
| A050 | pass: wt/test-m1u6 is exactly 2f87e0b, wt/spike-m1u6-handle exactly b896d2e and wt/spike-m1u6-signal exactly 3066404; the 23/23 figure is unverifiable and sits in the Register. | `git rev-parse --short=7` over the three branches |
| A051 | fail(med): roadmap records .agent/review-m1.md as committed and read first by every resumed MILESTONE-REVIEW session; the path does not exist at 9020e62 and is absent from the commit. | `git cat-file -e HEAD:.agent/review-m1.md` -> fatal: path does not exist in HEAD; `ls` -> No such file |
| A052 | pass: every ratio reproduces — 226/130 = 1.74, 224/120 = 1.87, 228/135 = 1.69, and their mean is 1.7667, which rounds to the recorded 1.77. | `python3` arithmetic over the roadmap's own main=/est pairs |
| A053 | pass: all three follow — 75 x 1.77 = 132.75 which rounds to the recorded 133 against an actual 133; 145 x 1.77 = 256.65 rounding to 257; and 209/124 = 1.69 as recorded. | `python3` arithmetic over the roadmap Calibration paragraph |
| A054 | pass: three of the five checkable teammate gauges reproduce exactly — map-m1u2 69% 166,576, spike-m1u4-gen 62% 147,730, test-m1u6 68% 164,242. spike-m1u5-lib reads 119,217 against 114K and res-m1u7 183,032 against 172K, both consistent with a gauge read at harvest while the agent kept running; main= is point-in-time so a session high-water cannot falsify it. | usage sums over ~/.claude/projects/-run-host-.../*/subagents/agent-a*.jsonl |
| A055 | pass: not re-derived — the planning session's transcript directory no longer exists, so neither the 183K/191K gauges nor the five-teammates-across-three-waves count can be checked. Seven sessions survive and none carries planning teammates. See Register. | `ls` over the project transcript root; surviving teammate sets are u2-u7 plus the current session |
| A056 | pass: the arithmetic holds — 6 kernel units (u1-u6, u7 is docs) x 35 = 210, and the u3+u4 fixed check sets total 45+24 = 69, which is exactly the row count the follow-on commit cd0de15 seeds into .agent/review-m1.md. | `python3` arithmetic; `git log --oneline main` cd0de15 subject |
| A057 | pass: every pinned version resolves exactly — swipl-wasm 8.0.7, svelte 5.57.0, vite 8.2.2, typescript 5.9.3, cytoscape 3.34.2. The 8.0.7 = SWI-Prolog 10.1.13 mapping is asserted live by the kb-live suite rather than by package metadata. | `node -e` version reads over node_modules; tests/kb-live.test.ts |
| A058 | pass: @types/cytoscape is absent from both package.json and node_modules, and typescript is declared ^5.9.3 and resolves to 5.9.3, so the TS-5.x cap holds. | package.json devDependencies; node_modules/@types listing |
| A059 | pass: every recorded number reproduces exactly at b935beb — svelte-check 319 FILES 0 ERRORS, vitest 3 files / 38 tests, vite 112 modules. The rc=0 half could not be reproduced because pnpm lint now fails under the current ESLint against the u1-era config; that is toolchain drift, not a defect at that commit. The section is also stale: it is labelled the last clean-cache run while the actual last run is u7's 355/171. | /tmp/audit-u1.log; historical replay at b935beb with the shared node_modules |
| A060 | pass: recorded two forced builds at 10.5s; observed pnpm kb:reproduce rc=0 in 8.741s wall under two concurrent peers, the same order of magnitude and if anything faster than recorded. | `time pnpm kb:reproduce` in /tmp/audit-repro-smoke.log |
| A061 | pass: recorded 1047 members = 1041 payload + 5 tag + tagmanifest; observed exactly 1047 real members, 1041 under data/, and 6 non-payload files = 5 tag entries plus tagmanifest-sha256.txt. | raw gzip+tar header walk over kb/cnl-ckc-kb-g952cc950a0c6.tar.gz (1481762 B) |
| A062 | pass: observed ustar magic "ustar\\0"+"00" on every header, exactly 5 type-L @LongLink pseudo-entries, one root dir, all regular files, mode 0000644, uid/gid 0, one mtime across all 1047 real members, gzip mtime field 0. | raw tar header walk; L headers alone carry mtime 0, real members share 15244311260 |
| A063 | pass: recorded 337 docs x 3 representations plus queries/ 4 .ace; observed 337 ace/, 337 pl/, 337 align/ and queries/ = 4 .ace + pl/ + answers/ + traces/ at 4 files each. | `.scratch/kb/data/guidelines/cdc-2022-opioid/` listing; 16 query files total |
| A064 | pass: bagit.txt declares BagIt-Version 1.0; release-manifest.tsv records swipl 9.2.9 and 952cc950a0c6; NOTICE carries rights profile redistributable. | .scratch/kb/bagit.txt, release-manifest.tsv, NOTICE |
| A065 | pass: kb:build emits the three artifacts into kb/generated (gitignored), and paths.mjs sorts the PAYLOAD-matched names and joins them with `% file:<name>` markers exactly as recorded. | tools/kb/paths.mjs:10,27,32; .gitignore:7; kb:build log line |
| A066 | pass: recorded pvm 3ae8d455d875 / qlf 62bc61cc7d0e / catalog a38e74d9f518; `pnpm kb:reproduce` rc=0 printed all three verbatim. | /tmp/audit-repro-smoke.log REPRODUCE_RC=0, 8.741 s |
| A067 | pass: recorded pvm 437132 B and qlf 2168708 B; observed byte-exact after a clean rebuild at 9020e62. | `python3` stat over kb/generated/kb.pvm and kb.qlf; kb:build log line agrees |
| A068 | pass: not re-measured — the 121 ms image and 238 ms qlf figures were taken outside vitest with a dedicated harness that is not committed. The structural half reproduces: both artifacts load at schema 1 with 337 documents on every gate run. See Register. | tests/kb-live.test.ts DOCUMENTS = 337; /tmp/audit-gate.log kb:build "schema 1, 337 documents" |
| A069 | pass: recorded swipl-bundle 6243055 B and swipl-bundle-no-data 2616873 B; both byte-exact in the installed swipl-wasm 8.0.7 dist. | `python3` stat over node_modules/swipl-wasm/dist/swipl/ |
| A070 | pass: recorded 9 schema predicates with counts 337/337/1834/1834/1254/2513/1003/16/1193; a static sweep over the 337 pl/ payload files returns exactly those nine predicates at exactly those counts. | `python3` regex count over .scratch/kb/data/guidelines/*/pl/*.pl |
| A071 | pass: the ban is real and holds — no JSON.stringify appears in src/engine or src/questions production code, and the corruption itself is a spike-era measurement not re-run. Byte-equality of all four committed answers is the standing proof that no such corruption reaches output. | `rg JSON.stringify src/` returns no production hit; tests/questions-live.test.ts:167-180 |
| A072 | pass: the write_canonical match is proven transitively — all four exported ids reproduce their committed canonical bytes through term_string/3 rendering. The 0.0437 ms/binding figure is spike-local and not re-measured. | tests/questions-live.test.ts:167-180; .agent/contracts/m1u2.md D1.3 |
| A073 | pass: .gitignore spells `node_modules` and `kb/generated` with no trailing slash, each carrying the symlink rationale as a comment. | .gitignore:2 and :7 |
| A074 | pass: swipl-wasm is imported by exactly one module, src/engine/worker.ts, and EngineSession takes its image loader through SessionOptions.loadImage, so the logic is testable without a DOM Worker. | `rg -c swipl-wasm src/engine/*.ts` -> worker.ts:1 only; src/engine/session.ts:61-62,122-124 |
| A075 | pass: not re-measured — the 0.681 ms catch and the +0.346 ms / +0.30% overhead came from a 30-run paired spike harness on wt/spike-m1u3-pl. The structural claims stand: the stack flag is lowered and restored per request and the typed resource_error state is gate-tested. See Register. | .agent/contracts/m1u3.md D1; tests/engine-budgets.test.ts |
| A076 | pass: not re-measured — threads=false and the library(time) / call_with_time_limit/2 / alarm/4 existence errors were observed in the wave-1 spike engine. The shipped design is consistent with them: wall-clock is enforced in JS on the main thread and no Prolog timer API is called. See Register. | .agent/contracts/m1u3.md D1 evidence; src/engine/budget.ts carries no time-library call |
| A077 | pass: not re-measured — the 100000 answers in 452.232 ms figure is a spike measurement. Its consequence is shipped and tested: the JS answer cap and deadline bound every query and no unbudgeted surface is exported. See Register. | .agent/contracts/m1u3.md D2; contract P3.5 and P6.2 |
| A078 | pass: not re-measured — the 25.97 ms main-thread versus never-fired in-worker timer comparison is spike-local. The ruling it produced is shipped: the hard deadline lives on the main thread, and the recorded wallClockMs + 500 ms offset is the deadline the client arms. See Register. | .agent/contracts/m1u3.md D3; src/engine/client.ts deadline arming |
| A079 | pass: not re-measured — the 62.00 ms worst step over 80 is a spike figure that the m1u6 probe corpus also fixes as not-to-be-re-derived. The macrotask-yield design it justifies is shipped and cancel is gate-tested. See Register. | .agent/contracts/m1u3.md D4; .agent/contracts/m1u6.md probe corpus seed |
| A080 | pass: swipl-wasm 8.0.7 ships close in no .d.ts; common.d.ts declares next and once alone, exactly as recorded, so Query.close() is undeclared runtime API. | `python3` scan of node_modules/swipl-wasm/dist/**/*.d.ts for close/next/once |
| A081 | pass: src/engine carries all three reserved names BudgetDepth_, BudgetInference_ and BudgetResource_, and bareTerm strips the trailing full stop before wrapping. | `rg` over src/engine/*.ts; src/engine/budget.ts:73 `goal.trim().replace(/\\.$/, "")` |
| A082 | pass: not re-measured — terminate, respawn and browser-floor timings are spike-local and the browser Worker.terminate() case is recorded as UNMEASURED and filed as a polish entry. The 337 -> 338 -> 337 overlay behaviour is the part the suite proves. | .agent/contracts/m1u3.md D5; .agent/polish.md:51-52 "Browser hard-kill proof" |
| A083 | pass: not re-measured — the ~2222464 KiB peak RSS and the 19.26 s runaway are spike figures. The typed state they justify is shipped as its own terminal outcome per contract P2.7. See Register. | .agent/contracts/m1u3.md D9; contract P2.7 |
| A084 | fail(low): 337 documents reproduces exactly, but neither static instrument reaches 800 nouns or 127 verbs — a clause-site census over the 337 pl/ files gives 476 distinct entity-type atoms and 63 event verbs, and the bag lexicon gives 1040 noun entries and 7 verb entries. The figures need the documented SWI co-load, which was not reproduced. | `python3` regex census over .scratch/kb/data/guidelines/*/pl/*.pl and data/guidelines/*/lexicon.ulex |
| A085 | pass: the static half reproduces exactly — 9 schema predicates, 1834 guideline_entity/4 clause sites and 1254 guideline_event/3 clause sites. The derivable counts 316 and 232 need a live co-load and were not re-derived. See Register. | `python3` static clause census; matches the A070 counts independently |
| A086 | pass: the seven explicit edge schemas are exactly the non-metadata predicates present (entity, cardinality, event, arg, pp, property, operator) out of the 9 total; the 9053 Horn body-to-head edge count needs a live extraction and was not re-derived. See Register. | `python3` predicate census over the 337 pl/ files |
| A087 | fail(low): the consult and QLF timings were not re-measured, but two recorded figures are checkable and both drift — the saved PVM is 437132 B at 9020e62 rather than 437,064 B, and the recorded 10.8s to build is contradicted by pnpm kb:reproduce completing TWO forced builds in 8.741s total. | `python3` stat over kb/generated/kb.pvm; /tmp/audit-repro-smoke.log REPRODUCE timing |
| A088 | pass: recorded concatenated pl/ = 4,716,517 B raw and 169,571 B gzip-9; recomputed over the 337 payload files gives exactly 4716517 B and 169571 B. | `python3` join + gzip.compress(level=9) over .scratch/kb/data/guidelines/*/pl/*.pl |
| A089 | fail(low): recorded 3,500,973 B raw with worker 3,062,652 B and PVM 437,064 B; the build at 9020e62 emits 14 files totalling 3773949 B with worker 3074247 B and PVM 437132 B. The figures were accurate pre-u7, before fonts and licences joined dist/. | `pnpm build` then `python3` rglob over dist/; /tmp/audit-gate.log build summary |
| A090 | fail(low): the Measured section still records the other three exported queries as "value-equal, not byte-proven", but the shipped suite compares all four with toBe against bag-read committed bytes, so all four are byte-proven. | tests/questions-live.test.ts:167-180 vs .agent/memory.md Measured "Oracle fidelity" bullet; memory u4 section already states the stronger claim |
| A091 | pass: not re-measured — the 796 ms recipe timing and the 4.03-5.09s meta-interpreter figures are spike-local. The recipe itself is shipped and green: the overlay probe injects a PID-unique clause and asserts the answer changes, importing no fixture. | tests/questions-live.test.ts overlay probe; /tmp/audit-gate.log 171 passed |
| A092 | pass: recorded generation artifact ~3.3 kB; kb/generated/question-catalog.json is 3337 B. The 0.631 ms and 1036.812 ms timings are wave-1 spike measurements and sit in the Register. | `python3` stat over kb/generated/question-catalog.json |
| A093 | pass: recorded queries/ absent from the PVM because PAYLOAD admits data/guidelines/*/pl/*.pl only; the regex has that exact shape and the extra path segment in queries/pl/ excludes all 16 query files. | tools/kb/paths.mjs:14 `/^data\\/guidelines\\/[^/]+\\/pl\\/[^/]+\\.pl$/`; kb:build reports 337 files |
| A094 | pass: recorded 24 sites for category-B-recommendation and 3 for evidence-type-3-recommendation; a byte scan over the 337 pl/ files returns exactly 24 and 3, and all three named near-miss atoms exist (6 / 1 / 7 sites). | `python3` bytes.count over the concatenated pl/ payload |
| A095 | pass: kb-manifest.json records manifestVersion 2 and carries a catalog block, exactly as recorded. | `python3` json load of kb/generated/kb-manifest.json; keys include catalog + manifestVersion 2 |
| A096 | pass: recorded scan scope src, tools, vite.config.ts, index.html; PRODUCTION_ROOTS is exactly that list and tests/ is excluded by a documented comment. | tools/kb/check.mjs:21 + :15-20 comment; asset-check log "answer-oracle scan clean over 4 roots" |
| A097 | pass: recorded eslint-plugin-svelte 3.23.0 with 86 rules and zero a11y-*; the installed plugin exports exactly 86 rules and 0 whose name starts a11y. | `node -e` import of eslint-plugin-svelte, Object.keys(rules).length = 86 |
| A098 | pass: axe-core resolves to exactly 4.13.0 and jsdom to exactly 29.1.1, both as recorded; the jsdom canvas gap shows up as 12 getContext warnings in the gate test run. | node_modules/{axe-core,jsdom}/package.json; /tmp/audit-gate.log getContext lines |
| A099 | pass: bits-ui and @internationalized/date are both absent from node_modules, which is the state D2 rejection implies; the 2.19.0 version and peer requirement are only checkable on wt/spike-m1u5-lib and sit in the Register. | `python3` existence check over node_modules/bits-ui and node_modules/@internationalized/date |
| A100 | fail(low): recorded "removing kb/generated/kb.pvm gives rc 1 via waitForSelector timeout"; with dist/ present the same removal gives rc 0, and with dist/ also removed rc is 1 from the execFileSync(pnpm build) step, not a waitForSelector timeout. | controls.sh C6 (rc=0); rerun with dist/ removed (rc=1, build throw); tools/smoke.mjs:143 |
| A101 | pass: smoke.mjs locates `[role="option"][id$="-option-${QUESTION}"]` and the combobox builds ids as `${uid}-option-${QUESTION_IDS[index]}`, so the recorded selector shape and ordering both hold. | tools/smoke.mjs option locator line; src/questions/QuestionCombobox.svelte optionId |
| A102 | pass: recorded literata alone is 1883680 B across 58 files; observed exactly 58 files totalling 1883680 B in the installed package (42 of them woff2 at 1848120 B). | `python3` rglob over node_modules/@fontsource-variable/literata |
| A103 | pass: recorded ten role tokens; :root defines exactly those ten (--surface, --surface-raised, --surface-sunken, --text, --text-muted, --border, --action, --action-text, --warn, --focus-ring) plus three --font-* tokens. | src/app.css:94-107 |
| A104 | pass: contrast.mjs floors with Math.floor(ratio*100)/100, throws on an empty pair table, and fails any non---font- token that appears in no pair. | tools/contrast.mjs:104, :112, :117 |
| A105 | pass: copy-check grades src/demo/copy.ts (INSTRUCTIONS 20, DESCRIPTIONS 25) and src/demo/describe.ts (25), and uses \\uE000 as the digit-period sentinel. | tools/copy-check.mjs:29-35, :72-74 |
| A106 | pass: recorded all 337 documents labelled unreviewed; release-manifest.tsv contains exactly 337 unreviewed occurrences across its 1394 lines. | `grep -c unreviewed .scratch/kb/release-manifest.tsv` = 337 |
| A107 | fail(low): recorded tools/probe-u7.mjs is 545 lines; the file on wt/map-m1u7 124e34d is 548 lines. | `git show wt/map-m1u7:tools/probe-u7.mjs |
| A108 | pass: all three required survivors are present — .scratch/agents/map-m1u1.md, .scratch/agents/spike-m1u1-det.md and .scratch/validate-report.py. | `ls` over the three paths in the primary tree |
| A109 | pass: wt/res-m1-1 resolves to exactly 36cc56f and wt/res-m1-2 to exactly 5863141. | `git rev-parse --short=7 wt/res-m1-1 wt/res-m1-2` |
| A110 | pass: .serena/project.yml declares language_servers exactly as ['svelte', 'typescript', 'markdown', 'json', 'yaml'], and .serena/ is committed (project.yml and .gitignore are both tracked). | .serena/project.yml:38; `git ls-files .serena` |
| A111 | fail(low): the roadmap names six tools/kb/*.mjs and all six exist, but the m1u1 C1 artifact table lists only four (bag, produce, build, check) and omits paths.mjs and reproduce.mjs, both of which ship and one of which backs the byte-reproducibility claim. | .agent/contracts/m1u1.md C1 table vs `ls tools/kb/` |
| A112 | pass: tsx, vite-node, jiti and esbuild are all absent from node_modules and vitest declares no vite-node dependency, exactly as D0.2 records. | `python3` existence checks under node_modules; vitest/package.json dependencies |
| A113 | fail(low): the assetsInlineLimit: 0 pin is real but sits at vite.config.ts:22, not :7; the hashed PVM is 437132 B at 9020e62, not the recorded 437064 B, which was the u2-era artifact size. | vite.config.ts:22; dist/assets/kb-mjdzunFE.pvm = 437132 B |
| A114 | fail(low): C1 names tests/engine-terms.test.ts and tests/engine-live.test.ts; neither exists at 9020e62 and the shipped suite is tests/engine-session.test.ts. The contract hedges paths as provisional, so the concern set survives while the two named paths do not. | .agent/contracts/m1u2.md C1 table + its "Paths are provisional" preamble; `ls tests/` |
| A115 | pass: not re-measured — the 197-line parser, 1-of-3 adversarial forms, 853 B helper and 38.7-81.6 ms consult are all spike-local on wt/spike-m1u2-pl. The ruling they produced is shipped as the JS decoder. See Register. | .agent/contracts/m1u2.md D1.1; wt/spike-m1u2-pl |
| A116 | pass: not re-measured — 0.0414 vs 0.0345 ms/query and the 94.65 ms blocked callback are spike figures. The shipped protocol delivers one correlated batch per query as D1.2 rules, and streaming remains a polish entry. | .agent/contracts/m1u2.md D1.2; src/engine/protocol.ts batch response |
| A117 | pass: not re-measured — every terminate, respawn and residue timing in D5 is spike-local across two branches. The two independent measurements are mutually consistent and the recreate behaviour they justify is gate-tested through client.reset. | .agent/contracts/m1u3.md D5; tests/engine-budgets.test.ts:417,435 |
| A118 | pass: both pointers resolve — tools/kb/produce.mjs carries the printErr and on_output sink at :79-80, inside the recorded :72-96 span, and src/engine/terms.ts declares decodeOnce at :125, inside the recorded :125-129 span. | tools/kb/produce.mjs:79-80; src/engine/terms.ts:125 |
| A119 | fail(low): m1u4 D1 records the catalog artifact at 2644 B; the shipped kb/generated/question-catalog.json is 3337 B. memory.md instead says ~3.3 kB, which matches, so the contract carries the stale spike-era figure. | `python3` stat over kb/generated/question-catalog.json vs .agent/contracts/m1u4.md D1 |
| A120 | pass: recorded 734 B in two LF-terminated lines with the live-derivable result as the 481-byte substring at [249,730); the committed file is 734 B, 2 newlines, and solutions([ opens at 249 and closes at 730 for a length of exactly 481. | `python3` offsets over .scratch/kb/data/guidelines/cdc-2022-opioid/queries/answers/category-a-recommendations.pl |
| A121 | fail(low): the ESLint pointer is exact — no-restricted-imports.js:858-864 is the ImportDeclaration/ExportNamedDeclaration/ExportAllDeclaration visitor set — but tools/kb/check.mjs is only 115 lines and its fail accumulator sits at :47-49, not the recorded :13-34,66-75. | node_modules/.pnpm/eslint@*/...:858-864; tools/kb/check.mjs line census |
| A122 | pass: every D8 figure reproduces exactly — Role product 10336 / context 8911, Locator ref(1..23) and box(1..9), Sentence 1..25, and 337 distinct document ids. | `python3` regex census over the concatenated 337 pl/ payload files |
| A123 | pass: wt/spike-m1u4-gen resolves to exactly 65f496a and wt/spike-m1u4-src to exactly c193a87, so both differential branches are intact and rerunnable. | `git rev-parse --short=7 wt/spike-m1u4-gen wt/spike-m1u4-src` |
| A124 | pass: matrix seeded at 2ca27a0 (commit present); verdict table is 26 rows; the recorded 342 files / 7 test files / 133 tests / 3 assets / 117 modules all reproduce exactly at 5e3bf46. | .agent/contracts/m1u5.md; /tmp/audit-historical.log 5e3bf46 block shows 117 modules transformed |
| A125 | pass: recorded map-m1u6 25/25, spike-m1u6-handle 10/10, spike-m1u6-signal 10/10; the validator grades all three at exactly those figures with 0 problems; base 5e3bf46 is a real commit. | `validate-report.py` x3; `git log --oneline` shows 5e3bf46 |
| A126 | pass: both diffstats reproduce exactly against base 5e3bf46 — alt B (signal) 3 files / +507 -5 with zero existing test files touched, alt A (handle) 4 files / +569 -71 with tests/engine-budgets.test.ts modified. | `git diff --shortstat` and `--name-status` 5e3bf46 vs wt/spike-m1u6-{signal,handle} |
| A127 | pass: not re-measured — the ten-trials-per-id budget probe and its 119.972 ms maximum are map-m1u6 measurements. The decision resting on them is shipped and gate-tested: DEMO_BUDGET is frozen with wallClockMs 5000. See Register. | .agent/contracts/m1u6.md D5; tests/demo-lifecycle.test.ts:358-359 |
| A128 | pass: recorded R10 six ids settle answer at 7/5/2/1/3/12; the committed test asserts that exact array against the real PVM and passed in my clean-cache gate run. | tests/demo-lifecycle.test.ts:372-390; 171/171 tests passed |
| A129 | pass: kb-manifest.json records input {files: 337, sha256: 021d1220c6a0ed0e...}, so both the 337-file count and the 021d1220c6a0 digest prefix reproduce at 9020e62. | `python3` json load of kb/generated/kb-manifest.json input block |
| A130 | pass: wt/map-m1u6 does not exist as a branch, and neither src/demo/DemoController.svelte.d.ts nor tools/smoke.sh is present in the primary tree at 9020e62. | `git rev-parse wt/map-m1u6` -> ABSENT; `ls src/demo/ tools/` |
| A131 | pass: package.json gate string equals the m1u7 gate identity exactly, including copy:check and contrast:check between kb:asset-check and format:check. | package.json `gate` script vs .agent/contracts/m1u7.md Predicates preamble |
| A132 | pass: recorded perturbing --text-muted gave rc 1 naming 3 failing pairs and restoring gave rc 0; observed exactly that, with the three named pairs on --surface, --surface-raised and --surface-sunken. | controls.sh C2: mutant rc=1 "contrast: 3 failure(s)", restored rc=0 |
| A133 | pass: recorded a 26-word probe gave rc 1 naming file, key and count; a 30-word INSTRUCTIONS probe gave rc 1 naming src/demo/copy.ts INSTRUCTIONS.auditProbe and "30 words, limit 20", rc=0 after restore. | controls.sh C1 |
| A134 | pass: not re-derived — the packages report fontsource tags v7 and v40, not upstream 2.001 and 3.103, so the font versions need a woff2 name-table read. The licence half checks out: all three packages declare OFL-1.1 and their LICENSE texts ship byte-equal. | node_modules/@fontsource-variable/*/metadata.json and package.json license fields |
| A135 | pass: tools/visual-qa.mjs is absent from the primary tree at 9020e62, so the recorded fail(low) for G4-G5 is accurate; the walker exists only as tools/probe-u7.mjs on wt/map-m1u7. | `ls tools/` in wt/audit-m1; `git show wt/map-m1u7:tools/probe-u7.mjs` resolves |
| A136 | pass: recorded the sibling scan matches ../cnl-ckc only at a name boundary; an injected ../cnl-ckc/ reach gives rc 1 while an injected ../cnl-ckc-demo/ reach gives rc 0. | controls.sh C3 vs C5; tools/kb/check.mjs:36 SIBLING regex negative lookahead |
| A137 | fail(low): recorded map-m1u7 5/26 at 169K and res-m1u7 4/16 at 172K; observed map-m1u7 filled 10 of 26 rows with transcript high-water 182,351 (76%), and res-m1u7 4 of 16 with 183,032 (76%). The wave-failure conclusion stands; both figures understate what the transcripts show. | `validate-report.py` over .scratch/agents/map-m1u7.md and res-m1u7.md; usage sums over agent-amap-m1u7-*.jsonl and agent-ares-m1u7-*.jsonl |

## Details



## Register



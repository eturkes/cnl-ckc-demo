# rev-m1u1-1 — adversarial review of M1.u1, export→PVM producer

Check set enumerated here, then adjudicated here. 30 rows. Sources = `.agent/contracts/m1u1.md`
(D0, C1, P1–P5, I, G, Q), `.agent/roadmap.md` u1, `.agent/memory.md` "Knowledge base" and
"Generated runtime payload (u1)", `CLAUDE.md`.
Grade: `python3 -P .scratch/validate-report.py .agent/review-m1/rev-m1u1-1.md --verdict`

Review target = `6132bc6`. Ids in the check-set table are backticked so the grader reads one row
per id from the verdict table alone.

| id | finding | evidence |
| --- | --- | --- |
| U01 | pass: sidecar digest is compared before `verifyBag` is called; flipped sidecar byte and flipped archive byte each abort rc=1 with the sidecar-mismatch message, never a tar/gunzip error, proving the gate precedes parsing. | `build.mjs:24-30`; probe/u01-sidecar.log, probe/u01-archive.log (rc=1 each), probe/u01-restored.log (rc=0) |
| U02 | pass: `bag.mjs` imports only `node:crypto`+`node:zlib` so the reader has no fs capability at all; no tar/decompress dependency in `package.json` or lockfile; a forced build changes exactly the four `kb/generated/` artifacts and nothing else in the tree. | `bag.mjs:5-6`; `rg tar package.json pnpm-lock.yaml` rc=1; probe/tree.before vs probe/tree.after diff = 4 mtime-only lines |
| U03 | fail(low): six of seven classes give distinct typed reasons (unsupported-typeflag/absolute-path/dotdot-segment/duplicate-name/multiple-roots/bad-size, 7/7 distinct incl. control-char), but the NUL class is not refused on the GNU long-name path — `readBag` truncates at the first NUL before `safeName` runs, so the member is ACCEPTED. | probe/u03-u04.log, probe/u03-nul.log; `bag.mjs:130-132` vs `bag.mjs:82`; `node probe/red-nul.mjs` rc=1 |
| U04 | fail(low): all lexical refusals report the offending name verbatim and rewrite nothing (8/8 hostile names REFUSED, prefix-split and long-name paths included), but a NUL-bearing long name is repaired by truncation into an accepted name, and a bag listing that truncated name verifies end to end. | probe/u03-nul.log "OUTCOME: bag VERIFIES"; `bag.mjs:130-132`; `node probe/red-nul.mjs` rc=1, rc=0 under the 2-line fix in probe/bag.mjs.orig diff |
| U05 | pass: `verifyBag` refuses `BagIt-Version: 0.97` and an absent version line (bagit-version), a missing declaration (bagit-missing), and verifies both manifests — a flipped tagmanifest digest and a flipped tag body both abort digest-mismatch; either manifest missing aborts manifest-missing. | probe/u05-u07.log U05 block; `bag.mjs:222-231` (version) and `bag.mjs:239-240` (both verifyDigests calls) |
| U06 | pass: completeness runs both ways — an extra unlisted payload file and an extra unlisted tag file both abort unlisted-payload (rejection, not a warning), a manifest entry with no member aborts missing-member, and the same builder yields ACCEPTED for the well-formed control. | probe/u05-u07.log U06 block (4 lines incl. CONTROL=ACCEPTED); `bag.mjs:242-249` |
| U07 | pass: three one-byte mutants on the real vendored bag each abort with a named cause — payload byte and manifest line give digest-mismatch, the bagit.txt byte gives bagit-version (version gate precedes digests); a sidecar-repaired payload mutant aborts the real CLI rc=1. | probe/u05-u07.log U07 block; probe/u07-e2e.log rc=1 "digest-mismatch: data/guidelines/cdc-2022-opioid/pl/cdc2022-opioid-rec01-imp01.pl" |
| U08 | fail(low): fixtures are all synthesized in-file (29 refusal cases, zero binaries under `tests/` — 10 files, all .ts), but the Q corpus census is 16/20: edited-manifest-digest, truncated-gzip, NUL-in-name and literal `a/../a/../a` have no case. The NUL gap concealed the live U03/U04 defect. | probe/u08-gaps.log; `tests/kb-bag.test.ts:115-260`; `find tests -type f` = 10 .ts files |
| U09 | pass: real bag yields exactly 1041 payload + 5 tagmanifest-listed tag entries + tagmanifest itself = 1047 members; an independent raw scan finds the 5 GNU `L` headers, and all 5 names over 100 chars (101-106) resolve to their full paths. | probe/u09-census.log; `bag.mjs:126-135` (L dispatch), `bag.mjs:137` (assembled name -> safeName) |
| U10 | pass: `payloadSource` filters `data/guidelines/*/pl/*.pl` and sorts, fixing order in code; an independent tar walk importing nothing from tools/kb recomputes 337 files / 4740529 B / sha 021d1220c6a0… identical to the manifest, and both reversed order and marker-free joins change the digest. | probe/u10-input.log MATCH=true; `paths.mjs:13,26-36`; manifest input.sha256 021d1220c6a0ed0e…17cf8d |
| U11 | fail(med): `requireLoaded` sits before `qsave_program` (position correct) but only asserts schemaVersion>=1 and documents>=1, so a 336-file corpus, a 1-file corpus and a schema-2 corpus all build and save; the only 337/schema-1 guard is a vitest case that runs after every artifact is on disk. | probe/u11-u12.log U11 block; `produce.mjs:143-151,165-171`; `node probe/red-p22.mjs` rc=1; `tests/kb-live.test.ts:12-13` |
| U12 | fail(low): capture and fail-closed are sound — user_error write, syntax error, undefined predicate and a discontiguous warning all abort, and the filter is scoped to image save alone; but SAVE_NOISE tolerates any `qsave.pl:<n>:` line, so a genuine save ERROR at that path is swallowed too. | probe/u11-u12.log U12 block (2 noise lines, 0 survivors); `produce.mjs:97,159,168`; SAVE_NOISE breadth demo matches "ERROR: …qsave.pl:99: cannot write state" |
| U13 | pass: one `--force` run emits kb.pvm (437132 B) and kb.qlf (2168708 B) plus the catalog; both come from the single `source` binding whose digest the manifest records once as input.sha256 021d1220c6a0…, all three on-disk digests match, and `requireSameContract` re-loads each artifact and refuses any contract drift. | probe/u13-run.log; `build.mjs:52-56,73-85`; manifest assets pvm 3ae8d455d875 / qlf 62bc61cc7d0e |
| U14 | pass: assets are written first and the manifest last through a tmp+rename; a build killed in that exact window (rc 137, 336-doc assets on disk) leaves the manifest byte-unchanged, and `kb:asset-check` then fails rc=1 naming both size mismatches rather than passing. | probe/u14-interrupt.log rc=137; probe/u14-check.log rc=1 "kb.pvm: 436512 bytes, manifest says 437132"; `build.mjs:89-92,119-123` |
| U15 | pass: `generateImageBuffer` appears nowhere in tools/, src/ or tests/ except the produce.mjs comment citing why it is not used; the four steps are re-implemented as boot(bundle, -q -f prolog.pl)+writeSource, contract assertion, qsave_program, FS.readFile, all under the caller-supplied printErr/on_output sink. | `produce.mjs:3,73-92,163-171`; `rg generateImage tools/ src/ tests/` = 1 comment hit |
| U16 | pass: caveat — G2 gives byte-identical manifests across two clean `--force` builds, so the nondeterministic field set is empty and `reproduce.mjs` compares in full rather than excluding anything; the named-exclusion surface P3.1 asks for does not exist in `src/kb/manifest.ts` because nothing needs excluding. | probe/g2-a.json vs probe/g2-b.json `cmp` identical; `rg exclude|nondetermin src/kb/manifest.ts` rc=1; `reproduce.mjs:22-26` |
| U17 | pass: `pnpm kb:reproduce` from committed state exits 0 with two forced builds byte-identical at pvm 3ae8d455d875 and qlf 62bc61cc7d0e, matching the memory.md values exactly; `withPinnedClock` wraps only buildImage/buildQlf, so verifyImage/verifyQlf run on the real clock. | `node tools/kb/reproduce.mjs` rc=0; `produce.mjs:57-66,160,181` vs unwrapped `produce.mjs:197,213` |
| U18 | pass: the cache hit is conjunctive over manifestVersion, input.sha256, source.sha256, swiplWasm and assetsIntact, and both digests are recomputed from the bag on every run before the check while assetsIntact re-hashes each asset — so it verifies every digest it skips rebuilding for. | `build.mjs:52-70,36-45`; two consecutive runs both print "kb:build cached — 337 files, input 021d1220c6a0" |
| U19 | pass: all five named triggers force a rebuild — changed input digest, changed swipl-wasm version, missing asset, asset digest mismatch and manifest schema mismatch — plus a sixth (changed source bag digest); the untouched control reports `cached`, so the probe is not rebuilding unconditionally. | probe run: 6 rebuilt / 1 cached control; `build.mjs:59-66` |
| U20 | pass: every P3.4 group is present and populated — manifestVersion 2, bag name+sha256, payloadFiles 1041, tagFiles 5, input files 337 / bytes 4740529 / sha256 021d1220c6a0…, swiplWasm 8.0.7, path+bytes+sha256+kind on all 3 assets, contract schema 1 / 337 docs; no group absent. | field census over kb/generated/kb-manifest.json (11 groups present, 0 absent); `build.mjs:94-116`; `src/kb/manifest.ts:17-52` |
| U21 | pass: caveat — every manifest number traces to a run-observed expression (payload/tags .length, names.length, Buffer.byteLength, sha256(), swipl-wasm package version, readContract from the live engine); the only writer literals are MANIFEST_VERSION and `bagitVersion: "1.0"`, the latter reachable only after verifyBag refuses any other version. | `build.mjs:94-116`; `produce.mjs:110-130`; literal scan: `src/demo/copy.ts:43` UI copy + `src/questions/humanize.ts:3` comment, neither a pass condition; `src/engine/session.ts:76` reads schema from the engine |
| U22 | pass: the committed live case boots the freshly built kb.pvm through verifyImage (bundleNoData, -x image.pvm) and asserts schemaVersion 1 and documents 337; 5/5 tests pass against artifacts rebuilt with --force in this worktree, and the same values appear in the build output and the manifest. | probe/u22-live.log rc=0 "Tests 5 passed (5)"; `tests/kb-live.test.ts:36-46`; `produce.mjs:197-206` |
| U23 | pass: caveat — the QLF case asserts the identical schema 1 / 337 values from kb.qlf and verifyQlf boots the large `swipl-bundle`, and D0.6 checks out exactly (6243055 B vs 2616873 B on disk); but there is no runtime switch to set — the "switch" is the exported verifyQlf plus an always-on test, so G3 as worded has no referent. | probe/u22-live.log (QLF case passes); `produce.mjs:213-215` uses bundle(); `ls node_modules/swipl-wasm/dist/swipl/` = 6243055 / 2616873; `rg FORCE_QLF|process.env tools/ src/ tests/` = no switch |
| U24 | pass: every expectation anchors to a live engine read or to the manifest — verifyImage/verifyQlf return engine-reported values compared against the contract constants 337/1, prolog version compared against the manifest; the file reads no fixture and no recorded answer, and no test compares a digest it computed from the artifact under test. | `tests/kb-live.test.ts:1-64` (imports only fs + tools/kb); `rg sha256 tests/kb-live.test.ts` = no hits |
| U25 | pass: with `kb/generated` removed the live suite fails rather than skips — 5 failed, 0 skipped, vitest rc=1 — and `kb:asset-check` exits 1 both with the directory absent and with it present but empty, reporting "no manifest … run pnpm kb:build". | probe/u25-test.log "Tests 5 failed (5)" rc=1; probe/u25-check.log + probe/u25-check-empty.log rc=1; `check.mjs:52-54` |
| U26 | pass: the gate runs kb:build then kb:asset-check ahead of copy:check, contrast:check, format:check, lint, check, test and build, so nothing later can pass on unverified artifacts; G1 from a clean `git archive` of 6132bc6 with kb/generated deleted exits 0 in 47s (svelte-check 355 files 0 errors, 177 tests, vite build -l warn ok). | `package.json:16`; /tmp/g1-rev-m1u1-1/gate.log rc=0 |
| U27 | pass: `check.mjs` imports bag/catalog/paths only, never produce.mjs, so no path can rebuild; it re-verifies every manifest asset against disk and recomputes the input digest from the bag; a flipped kb.pvm byte gives rc=1 "digest does not match the manifest", and artifact mtimes are byte-identical before and after a clean run. | `check.mjs:8-12,57-88`; probe/u27-tamper.log rc=1; probe/u27-mtime.before vs .after identical |
| U28 | pass: SCAN_ROOTS is exactly P5.3 six paths; the scan reports clean over 6 roots, a planted `../cnl-ckc` line in tools/kb/paths.mjs makes it fail rc=1 ("sibling path in tools/kb/paths.mjs"), no repo file outside .agent/ and CLAUDE.md matches, and G1 passed in /tmp where the sibling does not exist (P5.4). | `check.mjs:14`; probe/u28-control.log rc=1 / probe/u28-clean.log rc=0; `ls -d /tmp/cnl-ckc` = No such file; /tmp/g1-rev-m1u1-1/gate.log rc=0 |
| U29 | fail(low): `.gitignore:7` has kb/generated, `.claude/settings.json` denies `Read(/kb/generated/**)` (enforced — a read of that dir was blocked), and the app uses the `@kb` Vite alias with `import pvmUrl from "@kb/kb.pvm?url"`; but `.serena/project.yml` `ignored_paths` is only [pnpm-lock.yaml, kb/*.tar.gz], so the second half of P5.5 pair is unset. | `.gitignore:7`; `.claude/settings.json` deny entry 7; `.serena/project.yml:113`; `vite.config.ts:17`; `src/engine/worker.ts:8` |
| U30 | pass: all 12 C1 paths exist with their declared kind; tools are ESM JSDoc-typed under `allowJs`+`checkJs` with `tools/**/*.mjs` in `include`; tsx/vite-node/jiti/esbuild and every tar library resolve ABSENT; format:check, lint and check all pass over the u1 files (not prettier-ignored) and the CLAUDE.md ledger finds no rule breach. | C1 census 12/12; `tsconfig.json:11-12,25`; `require.resolve` probe 7/7 ABSENT; `prettier --check` on the 9 authored files rc=0; /tmp/g1-rev-m1u1-1/gate.log svelte-check 355 files 0 errors |

## Check set

| id | check | source | how to decide it |
| --- | --- | --- | --- |
| `U01` | mechanical · Does `kb:build` verify the archive against its `.sha256` sidecar before parsing any archive byte, and abort nonzero on mismatch? | P1.1 | Read the call order in `tools/kb/build.mjs` and `tools/kb/bag.mjs`; flip one sidecar byte and one archive byte, observe rc and the message each time. |
| `U02` | mechanical · Is the archive parsed entirely in memory, with no code path writing a member to the filesystem, through a purpose-built reader rather than a sanitizing tar library? | P1.2, D0.3 | Scan `tools/kb/` for write APIs and for any tar dependency; run `pnpm kb:build` under a probe that traces `fs` writes and confirm only the three `kb/generated/` artifacts appear. |
| `U03` | mechanical · Does the reader reject each of the seven named entry classes with a distinct typed reason — non-regular type, absolute path, `..` segment, NUL/control in name, duplicate name, resolves outside root, size inconsistent with the stream? | P1.3 | Synthesize one bag per class; assert the reason strings are pairwise distinct and typed, not a shared generic error. |
| `U04` | judgment · Is rejection refusal rather than repair — is no rejected name rewritten, stripped or normalized into an accepted one? | P1.4, D0.3 | Read every name-handling path for strip/normalize/resolve-and-continue; feed `a/../a/../a` and a leading-slash name and confirm neither is accepted in any rewritten form. |
| `U05` | mechanical · Does BagIt verification assert `BagIt-Version: 1.0` and verify every entry of both `manifest-sha256.txt` and `tagmanifest-sha256.txt`? | P1.5 | Read the verifier; run a `BagIt-Version: 0.97` fixture and a flipped tagmanifest digest, both must abort. |
| `U06` | mechanical · Is verification complete in both directions — every `data/` payload member listed, every manifest entry present — with an unlisted payload file rejected rather than warned? | P1.6 | Two synthetic bags: one extra unlisted payload file, one manifest entry with no archive member; both must abort nonzero. |
| `U07` | mechanical · Does a single flipped byte in any payload file, any tag file, or any manifest line abort the build nonzero? | P1.7 | Three one-byte mutants through the real verifier, each rc nonzero with a named cause. |
| `U08` | judgment · Does the committed suite cover the 20 hostile inputs the Q corpus names, with every fixture synthesized by the test rather than committed as an opaque binary? | Q corpus, I "hostile-bag fixtures are synthesized" | Census `tests/kb-bag.test.ts` case names against the Q list; confirm no binary fixture is committed under `tests/`. |
| `U09` | mechanical · Does the reader handle the five GNU `././@LongLink` (`L`) headers the real bag carries, which a resolving parser hides and a naive raw reader refuses? | `.agent/memory.md` Knowledge base | Read the header dispatch in `bag.mjs`; confirm the real bag yields 1041 payload + 5 tag entries + tagmanifest = 1047 members and that names over 100 chars resolve. |
| `U10` | mechanical · Are the 337 `pl/` payload files fed in a deterministic order fixed by the code, with that order's identity recorded in the manifest as a digest of the exact concatenated input? | P2.1 | Read the sort and the `% file:<path>` join; recompute the concatenation digest independently and compare against `input.sha256`. |
| `U11` | mechanical · Does the producer assert schema 1 and exactly 337 `guideline_document/3` documents inside the building engine before `qsave_program` runs, and abort nonzero otherwise? | P2.2 | Read the assertion's position relative to the save call; drop or corrupt a payload file and confirm the build aborts before any artifact is written. |
| `U12` | mechanical · Is engine diagnostic output captured during consult, with any error diagnostic aborting nonzero, and are the two `library(shlib)` warnings the only tolerated noise, tolerated at image save alone? | P2.3, D0.4, `.agent/memory.md` | Read the `printErr`/sink wiring; inject a payload clause that emits a diagnostic and confirm rc nonzero; confirm the runtime-consult path treats the same text as fatal. |
| `U13` | mechanical · Does one run emit both `kb.pvm` and `kb.qlf`, neither produced from an input different from the digest the manifest records? | P2.4 | Single-run artifact census; cross-check both asset entries against the one recorded `input.sha256`. |
| `U14` | judgment · Is the manifest written last, after every asset is on disk, so an interrupted build never leaves a manifest claiming artifacts it did not produce? | P2.5 | Read the write ordering; kill a build between asset write and manifest write and confirm the surviving state fails `kb:asset-check` rather than passing. |
| `U15` | mechanical · Is `generateImageBuffer` absent from every code path, with its four steps re-implemented under a caller-supplied diagnostic sink? | D0.4 | Scan `tools/` and `src/` for the symbol; read `tools/kb/produce.mjs` for the four steps and the sink. |
| `U16` | mechanical · Do two consecutive `kb:build --force` runs from a clean cache emit contract-equivalent manifests, with nondeterministic fields named explicitly in `src/kb/manifest.ts` and excluded by name rather than by a blanket comparison? | P3.1, G2 | Run G2; read the exclusion list in `src/kb/manifest.ts` and confirm it names fields rather than pattern-matching them. |
| `U17` | mechanical · Does the clock pin make two forced builds byte-identical at the recorded digests, and does `pnpm kb:reproduce` prove it from committed state? | `.agent/memory.md` Generated runtime payload | Run `pnpm kb:reproduce`; compare against `pvm 3ae8d455d875` / `qlf 62bc61cc7d0e`; confirm `withPinnedClock` scopes the pin to the build phase. |
| `U18` | mechanical · Does a second `kb:build` without `--force` skip the rebuild only when input digest, toolchain version and asset digests all still match, while still verifying every digest it skips rebuilding for? | P3.2 | Read the cache decision; run twice and confirm the second run reports a cache hit yet still recomputes and compares each digest. |
| `U19` | mechanical · Is a cache hit invalidated by each of the five named triggers — changed input digest, changed `swipl-wasm` version, missing asset, asset digest mismatch, manifest schema mismatch? | P3.3 | One probe per trigger; each must force a rebuild rather than pass on stale artifacts. |
| `U20` | mechanical · Does the manifest record every field group P3.4 names, at minimum: schema version; bag filename and sha256; BagIt payload and tag counts; input file count, byte count and concatenated sha256; `swipl-wasm` version; per-asset path, bytes, sha256 and kind; and the live-verified schema version and document count? | P3.4 | Field census of the emitted `kb-manifest.json` against the P3.4 list, naming any absent group. |
| `U21` | judgment · Is every manifest number observed during the run that wrote it, with 337 and the schema version read from the engine rather than hard-coded as a pass condition in production code? | P3.5, I "never hard-coded" | Trace each numeric field to its producing expression; scan `tools/` and `src/` for the literals and rule on every hit. |
| `U22` | mechanical · Does a freshly built `kb.pvm`, loaded through the documented loader, report schema version 1 and exactly 337 documents? | P4.1 | Run the committed live case against the real artifact and read the reported values. |
| `U23` | mechanical · Does the forced-QLF path satisfy the identical assertions from `kb.qlf`, behind a switch that is explicit and testable, and is the engine split D0.6 states a shipped fact? | P4.2, D0.6, G3 | Locate the switch; run G3 with it set; confirm the QLF path boots the large bundle and reports the same contract values. |
| `U24` | mechanical · Is the live contract asserted by a committed test that boots the real generated artifact, reading no fixture, recorded answer or stand-in constant, and does no test assert a digest it computed from the artifact it verifies? | P4.3, I "no test asserts a digest constant" | Read `tests/kb-live.test.ts` and trace every expectation to its anchor; flag any self-computed digest comparison. |
| `U25` | mechanical · Does the live test fail rather than skip when `kb/generated/` is absent, and can `kb:asset-check` never pass on an empty `kb/generated/`? | P4.4, I "cannot pass when empty" | Remove `kb/generated` and run both `pnpm test` and `pnpm kb:asset-check`; require nonzero and zero skips from each. |
| `U26` | mechanical · Does `pnpm gate` run `kb:build` then `kb:asset-check` ordered so no later member can pass on unverified artifacts, and is the gate rc 0 from a clean checkout with `kb/generated/` deleted? | P5.1, P5.6, G1 | Read the `gate` script order in `package.json`; run G1 from a clean extraction of the reviewed commit. |
| `U27` | mechanical · Does `kb:asset-check` re-verify manifest↔disk digests and recompute the input digest from the vendored bag without ever rebuilding? | P5.2 | Read the script for any produce call; tamper one asset byte and confirm it fails; confirm artifact mtimes are unchanged across a run. |
| `U28` | mechanical · Is the sibling-path scan empty over exactly the six paths P5.3 scopes, does the pipeline succeed with the sibling absent, and does no code path reference `../cnl-ckc`? | P5.3, P5.4, G4 | Compare the implemented scan roots against P5.3's list; run the scan; plant a positive control that must fail; grep for the sibling path outside `.agent/` and `CLAUDE.md`. |
| `U29` | mechanical · Are the generated paths in `.gitignore` and in both halves of the synced read-exclusion pair, and does the app reach them through the Vite alias with a `?url` image import? | P5.5, D0.5 | Read `.gitignore`, `.claude/settings.json` `permissions.deny`, `.serena/project.yml` `ignored_paths`, and the alias plus import site. |
| `U30` | judgment · Do the C1 artifacts all exist with their declared kind, are the tools ESM JSDoc-typed under `allowJs`/`checkJs` with no new dependency and no TypeScript runner, and does every touched durable file pass a per-file `CLAUDE.md` ledger? | C1, D0.1, D0.2, `CLAUDE.md` Authoring and Engineering | C1 path census; read `tsconfig.json` include and the dependency delta; adjudicate the ledger; confirm `format:check`, `lint` and `check` pass over the u1 files. |

## Details

One `### <row id>` section per row needing a ruling. Omit sections for clean `pass` rows.

## Register

Out-of-contract observations, each with an evidence pointer and a concrete acceptance check.

# rev-m1-cross-1 — cross-cutting review of M1 — integration + CLAUDE.md conformance

Check set = `.agent/contracts/m1-cross-rev-checkset.md` (fixed, 24 rows). Verdict table below; detail sections keyed by row id.
Fill every row IN PLACE; never renumber.
Grade: `python3 -P .scratch/validate-report.py .agent/review-m1/rev-m1-cross-1.md --verdict`

| id | finding | evidence |
| --- | --- | --- |
| X01 | pass: 21 modules form 43 relative edges with zero cycles or upward edges; `main` imports only `App`, which owns the demo seam | `src/main.ts:3`; import-graph command in `### X01` |
| X02 | fail(low): current sources carry no oracle or question literals, but the answer-oracle scan omits runtime-loaded `kb/generated` production assets | `tools/kb/check.mjs:21`; primary `kb/generated/` scan in `### X02` |
| X03 | fail(low): all 12 request/response arms clone cleanly and share protocol declarations, but committed coverage clones only one query response | `src/engine/protocol.ts:79`; `tests/engine-session.test.ts:279` |
| X04 | pass: one catalog id selects the displayed question and reaches `EngineClient.query` unchanged; the paired generated goal supplies the run bytes | `src/questions/QuestionCombobox.svelte:200`; `src/questions/service.ts:45-46` |
| X05 | pass: all six answer outcomes have producers and distinct render branches; every switch closes with an exhaustive `never` default | `src/questions/service.ts:19-27`; `src/demo/describe.ts:57-95` |
| X06 | pass: abort propagates through controller, service, client, worker and session; tests prove one cancel post, listener removal and result-owned settlement | `tests/engine-cancel.test.ts:C3-C6`; `tests/demo-lifecycle.test.ts:R6` |
| X07 | pass: every exported query-capable surface requires `BudgetSpec`; the UI alone calls it with the frozen `DEMO_BUDGET`, and omission fails validation | `src/demo/DemoController.svelte.ts:25-31,163`; `src/engine/client.ts:203-211` |
| X08 | fail(low): the `&&` gate fails closed and out-of-gate reruns remain runnable, but memory records an obsolete chain that omits two current checks | `.agent/memory.md:37-51`; `package.json:14` |
| X09 | pass: current main builds 14 files at about 3.77 MB, with zero engine markers in the main chunk and separate worker/PVM assets | `.agent/memory.md:242-249`; build census in `### X09` |
| X10 | pass: node and DOM projects classify all 16 committed test files exactly once; the suite runs 228/228 and current gate memory reports 16/228 | `vite.config.ts:27-44`; `.agent/memory.md:68-71` |
| X11 | pass: generated outputs are ignored, none are tracked, and slashless `node_modules`/`kb/generated` rules match real worktree symlinks | `.gitignore:1-24`; `git check-ignore -v node_modules kb/generated` |
| X12 | fail(low): deny and Serena sets are correctly partitioned and enforcement works, but `Read(/coverage/**)` matches no real path in the current tree | `.claude/settings.json:3-13`; `.serena/project.yml:99-102` |
| X13 | fail(med): core hashes and current gate shape reproduce, but two claims are stale and several runtime timings lack named instruments or browser counterparts | `.agent/memory.md:120-121,170,188-193,281-289` |
| X14 | fail(med): project `CLAUDE.md` is dense and rule-focused, but 493-line memory retains review provenance, correction stories and package/code duplication | `.agent/memory.md:19,43-71,193,203,340`; `CLAUDE.md:1-57` |
| X15 | fail(med): calibration arithmetic is correct, but roadmap omits session 6, lists three fixed check sets absent and disagrees with the ledger by 34 adjudicated rows | `.agent/roadmap.md:156-184`; `.agent/review-m1.md:18-33` |
| X16 | fail(med): all 29 polish entries now carry acceptance and priority, but one is already closed and three roadmap `NOT verified` items have no open entry | `.agent/polish.md:16-19`; `.agent/roadmap.md:60-62,97-98,151-154` |
| X17 | fail(low): six unit contracts match shipped review fixes, but m1u1 P5.5 still requires gitignored generated bytes in Serena contrary to the project rule | `.agent/contracts/m1u1.md:79-85`; `.agent/review-m1.md:187` |
| X18 | fail(low): all 42 M1-era subjects state cause and fix, but seven omit the `(M1…)` trace and disappear from the documented grep's 35 hits | `git log main 79233c3^..main`; `.agent/roadmap.md:256` |
| X19 | fail(low): domain render/budget/error logic is properly distinct, but built-browser setup and cleanup remain duplicated between `smoke` and `browser-check` despite a shared module | `tools/smoke.mjs:53-109`; `tools/browser-check.mjs:173-279` |
| X20 | fail(low): non-obvious decisions usually carry reasons, but at least seven comments retain review provenance and ten file headers merely restate their module's purpose | `tools/engine-check.mjs:1,26,136`; `tools/kb/check.mjs:40,51` |
| X21 | fail(med): shipped UI copy observes the human/machine boundary and ASD register, but the repository has no human-facing README or entry document | `src/demo/copy.ts`; `git ls-tree --name-only main` |
| X22 | fail(low): required pins and sections hold with no deprecations, but `cytoscape` is an unused production dependency while M3 remains unplanned | `package.json:27-50`; `git grep -F cytoscape main -- src tools tests` rc 1 |
| X23 | fail(med): a clean clone has no committed setup path, Node/pnpm prerequisite, package-manager version or install/run command; execution cannot begin as written | root tree census; `package.json:1-25` |
| X24 | fail(med): validator, mutator and visual walker have rerun/port records, but five browser claims and two research branches lack a scheduled committed evidence port | `.agent/memory.md:281-318`; `.agent/polish.md:68-75,95-99` |

### X01

Checked every relative `import`/re-export in 21 `src/**/*.ts|svelte` modules with a Python graph walk. Observed `files=21 edges=43 upward=0 cycles=0`; the only relative edge from `src/main.ts` is `src/App.svelte`. Alias edges are downward (`engine`/`questions` → `@kb`), while `App` composes `demo` and `questions` without importing `engine`. Command: `python3 -c '<import graph walk>'` from the worktree.

### X02

Re-derived all six catalog records in memory from the vendored bag, then scanned 34 files under the four declared production roots. Observed `oracle_literal_hits=0` and `question_literal_hits=0`; the render path remains `PlSolution.display`/decoded bindings → `answerRows` → `AnswerPanel` (`src/demo/describe.ts:144-158`). A separate byte scan over the primary tree's four current generated assets also returned rc 1 (no `queries/answers`), with `catalogVersion` rc 0 as the positive control.

Failure = coverage, not a current oracle hit. `PRODUCTION_ROOTS` omits `kb/generated` (`tools/kb/check.mjs:21`), although `src/questions/catalog.ts:5` runtime-imports its catalog and the worker imports its PVM. Prior u4 row `c19` covers source-import negative controls; its report registered, but did not adjudicate, this generated-root seam. A runtime assembly such as `['queries','answers'].join('/')` also evades a byte scan; no such production assembly exists now.

Severity: low. Impact: a generated production asset can acquire the forbidden byte without this gate step naming it; current generated bytes are clean and `kb:reproduce` independently limits the blast radius. Acceptance check: add `kb/generated` to the answer-oracle coverage, plant a generated file containing the literal and require `pnpm kb:asset-check` rc 1, then restore and require rc 0.

### X03

Censused transport declarations across `src/engine/**` and `src/demo/**`: `BudgetSpec`, `EngineRequestBody`, `EngineRequest`, `EngineResponse`, `EngineError`, `LimitKind` and `SolveResult` originate in `src/engine/protocol.ts`; client, session, worker and demo import or derive them. A Node sweep constructed all four request arms and all eight response arms, including nested solution terms, then compared each with `structuredClone`; observed `structuredClone=12/12 boot,query,consult,cancel,booted,solutions,failure,limit,cancelled,ack,consulted,error`.

Failure = committed coverage. `/usr/bin/rg -n structuredClone tests` finds one site, `tests/engine-session.test.ts:279`, where a single live query response is cloned. Thus 11 arms have no explicit committed clone sweep; even the query request is posted into an in-process test double rather than cloned by that assertion.

Severity: low. Impact: every shipped member is clone-safe now, but adding a non-cloneable field to an untested arm can leave the gate green until that arm crosses a real worker boundary. Acceptance check: commit a table-driven test containing all 12 discriminants, clone and deep-compare each, and show a non-cloneable-field mutant in any row makes the test fail.

### X04

Traced `recommendation-exists` and the generic id path. `QuestionCombobox` renders `QUESTION_CATALOG[selected].question` byte-for-byte (`src/questions/QuestionCombobox.svelte:200`); `App` passes that id to `DemoController.select`; `run` retains the id; `DemoController` calls `ask(id, DEMO_BUDGET, signal)` (`src/demo/DemoController.svelte.ts:163`); `AnswerService` reads the same `QUESTION_CATALOG[id]` and passes `entry.goal` unchanged to `EngineClient.query` (`src/questions/service.ts:45-46`). Running-status copy also reads the same catalog question (`src/demo/describe.ts:122`).

The bag replay produced six paired records. Exported pairs come from one query file; each repo-authored pair applies one guarded atom substitution to both question and goal (`tools/kb/catalog.mjs`). No hop re-cases, re-wraps or reconstructs either string. Answer humanization occurs only after the matching goal returns and uses that result's binding plus engine display, so it cannot switch the question that ran.

### X05

Enumerated `AnswerResult`'s six members and traced each producer in `AnswerService`/controller: `answer`, `failure`, `limit`, `cancelled`, `error`, and invalid-id `rejected`. `describeResult` has one branch per member. Answer/failure, each limit kind, cancellation, errors and rejection have distinct status/summary/error text; the error code and limit discriminant remain visible. Both production switches end in `const exhaustive: never`, not a semantic catch-all. The cancellation/lifecycle targeted run was 2 files / 24 tests, rc 0.

### X06

Traced `AbortController.abort()` at `DemoController.cancel` through `AnswerService.ask`, `EngineClient.query`, private `cancel(target)`, the worker's cancel branch and `EngineSession.requestCancel`. `EngineClient` guards the listener with `cancelSent`, posts the query before arming it, removes it in `#settle`, and treats the cancel `ack` as a separate pending request. The query's later `cancelled` response owns the UI result.

Ran `pnpm exec vitest run tests/engine-cancel.test.ts tests/demo-lifecycle.test.ts --reporter=dot`: 2 files / 24 tests passed. C4 observes exactly one cancel post after two abort calls; C6 observes one listener removal and no late post; C3 drives the real session to a partial cancelled result; R6 proves `cancelling` waits for and renders the query result rather than the ack.

### X07

Censused exported surfaces reaching `EngineClient.query`: `EngineClient.query(goal, budget, signal)`, `AnswerService.ask(id, budget, signal)`, `DemoEngine.ask(id, budget, signal)`, and `DemoController.run/retry` through its private dispatch. Every typed query/ask surface requires `BudgetSpec`; `EngineClient` validates it before posting, so a JavaScript caller that omits it receives typed `budget` error rather than an unbounded run. The only UI dispatch passes the frozen `DEMO_BUDGET` object. Lifecycle R9 asserts object identity and absence of controller goal/consult escape hatches; the targeted run passed.

### X08

`package.json` defines one nine-step `&&` chain: build assets, asset/copy/contrast checks, formatting, lint, type check, tests, production build. No pipeline or subshell masks a status. Out-of-gate reruns remain addressable: `pnpm kb:reproduce` ran rc 0 and reproduced all three hashes; `pnpm smoke` is a committed script; `wt/map-m1u7` and `wt/rev-m1u3-4` still exist for the two browser walkers; the primary `.scratch/verify-fixes.py` exists at its recorded path.

Failure: `.agent/memory.md`'s fenced `pnpm gate` expansion omits `pnpm copy:check` and `pnpm contrast:check`, yet its nearby measurements are presented as the current chain. Package reality is nine steps; memory states seven. Severity: low, because invoking `pnpm gate` still runs the correct package script, but a future agent reasoning from the durable expansion can misclassify both checks as out of gate. Acceptance check: make the memory expansion byte-match `package.json` and name the same nine steps in order.

### X09

Built in the review worktree with the current shared toolchain and generated assets. Observed 14 regular files / 3,774,068 file bytes, main-engine identifier hits 0 and worker hits 487. A read-only census of MAIN's current built `dist` observed 14 files / 3,774,137 B; the difference was a 69 B CSS delta while this review was concurrent with the u7 fix. Both satisfy the current durable claim: 14 files, approximately 3.77 MB, dominated by a separate ≈3.07 MB worker and 437,132 B PVM.

MAIN commit `bc3576f` retired the drift-prone exact total, records `du -sb dist` as its instrument, and marks the old 3,500,973 B / 1,339,723 B gzip total explicitly pre-u7. The split and stale-label predicates therefore pass at current `main`.

### X10

Current `main` tracks 16 test files. The node project includes `tests/**/*.test.ts` and excludes `*.dom.test.ts`; the DOM project includes exactly `tests/**/*.dom.test.ts`. Classification = 13 node + 3 DOM, with zero file excluded by both. I materialized current-main's added `tests/engine-recovery.test.ts` temporarily, linked the generated payload read-only, and ran `pnpm exec vitest run --reporter=dot`: 16/16 files and 228/228 tests passed, no skipped-file line.

MAIN commit `bc3576f` refreshed the active Gate claim to `vitest 16 files / 228 tests` and labels it M1-close shape that must be rerun rather than quoted. The `58 tests / 330 files` line remains inside the explicitly unit-scoped u2 history, not as the current suite total. Current discovery, execution and reporting agree.

### X11

Read every `.gitignore` rule, checked tracked ignored files with `git ls-files -ci --exclude-standard` (empty), and inspected ignored status after builds. Real worktree `.vite/` and `node_modules` are ignored. A temporary `kb/generated` symlink was also ignored; `git check-ignore -v` identified the slashless rules at `.gitignore:2,7`, proving the memory rationale against actual symlink objects. Primary-tree `dist`, `.scratch`, `.vite`, local settings and Headroom marker exist and match their classes. Dormant `coverage/`, `.probe/` and `CLAUDE.local.md` rules name documented optional outputs/overrides rather than stale tracked artifacts. Worktree status returned clean after every temporary artifact was removed.

### X12

Parsed `.claude/settings.json` as 326 B / 9 deny entries and `.serena/project.yml` as two `ignored_paths`. The partition is correct: tracked, non-gitignored `pnpm-lock.yaml` and `kb/*.tar.gz` appear in both; gitignored cache/generated/local paths appear only in deny. `.serena/.gitignore` owns `/cache`, so `ignore_all_files_in_gitignore: true` excludes it without duplication. A primary-root `Read(pnpm-lock.yaml)` was denied, while `Read(CLAUDE.md)` succeeded.

Failure: disk census found real paths for 8 of 9 deny entries; `coverage/` does not exist anywhere in the current primary tree. That breaches X12's explicit every-rule-matches-a-real-path predicate even though the rule is semantically plausible for future coverage output. Severity: low. Impact: the synced-pair claim overstates present disk correspondence but denies no required-readable path. Acceptance check: either create and document the coverage producer/path, or remove `coverage/` from both `.gitignore` and deny until such output exists; then require 9/9 real matches and repeat the denied/readable controls.

### X13

Walked current-main memory by section and reproduced its high-value live claims with package/source inspection, `pnpm kb:reproduce` (all three hashes), a clean build (14 files, main split clean), the complete Vitest census (16/228), the six-record bag catalog, and the 548-line walker branch. Package pins, PVM/QLF sizes, catalog hashes, current gate shape, current test shape and generated-asset split agree.

**Stale:** `Engine runtime (u2)` still says `Gate rc=0 at 58 tests / 330 files` (`.agent/memory.md:170`); current M1-close shape is 16 files / 228 tests and 366 checked files. The unit heading explains provenance to a careful reader, but the sentence itself asserts an unqualified gate total. Scratch conventions also says `Three files` must survive, then names four (`map-m1u1.md`, `spike-m1u1-det.md`, `validate-report.py`, `verify-fixes.py`; `:281-285`).

**Unverifiable as written:** no command, script, row or log is named for image/QLF live load 121/238 ms; display 0.0437 ms/binding; stack trip 0.681 ms and +0.346 ms/+0.30%; 100000-solution repeat 452.232 ms; worker/main timer 249.80/25.97 ms; corpus load 2806/3299/3578/213+724/335 ms; anti-hard-coding 796 ms; or catalog generation median 0.631 ms. These exact numbers cannot be regenerated from memory alone.

**Node figures without a browser counterpart:** the load, display, stack/overhead, repeat, timer, corpus-load and anti-hard-coding measurements above. The cancellation/restart and heap sections do carry paired browser results; browser per-step granularity is explicitly marked unmeasured. **Unrun by this review:** full `pnpm gate`, `pnpm browser:check`, `pnpm smoke`, heap/hard-cancel probes, the SWI clause census and the 45-mutant runner; their commands or evidence branches are named, so they are unrun rather than unverifiable.

Severity: med. Every future session reads this file, so one stale total plus instrument-free exact measurements repeatedly convert durable state into archaeology. Acceptance check: remove or explicitly historicize 58/330, attach a runnable instrument/evidence pointer to every exact measurement above, and pair each product-relevant Node runtime number with browser data or an explicit browser-unmeasured label.

### X14

`CLAUDE.md` is 57 lines / 11,574 B, dense, generalized and future-facing. A provenance/date/discovery scan found no authored violation; matches were the rules that prohibit those forms. It does not duplicate project implementation state.

Memory is 493 lines / 32,882 B and fails the same rules. Provenance/correction narrative includes `Rejected:` (`:19`), review-row origin stories (`:43-71`), `The 62.00 ms figure had no source` (`:193`), `8 cascading failures before it was wired` (`:203`) and `The rejected alternative` (`:340`). No calendar date was found. Duplication includes exact dependency versions already owned by `package.json`, the full gate already owned by its script, protocol/worker implementation already stated in code comments, and volatile gate/build/test totals already owned by commands and history.

Severity: med. The content is mostly technically valuable, but history and duplication inflate every session's attached state and helped stale X13/X15 claims survive. Acceptance check: retain only constraints, non-obvious current facts and runnable evidence; delete rejected-option/correction stories, package/script restatements and unit-era totals; move closed-review narrative to `.agent/archive/`.

### X15

Recomputed calibration: 226/130=1.74, 224/120=1.87 and 228/135=1.69; mean=1.7667→1.77. Then 75×1.77=132.75→133 and 145×1.77=256.65→257. Those arithmetic claims pass. Recorded gauges exist for u1, u2, u4-u7; u3 has no unit gauge.

Current ledger coverage sums to 354 adjudicated rows: u1-u7=209, audit=137, cross-cutting=8. Roadmap stops at session 5's historical 320 and has no session 6 record, so it omits u7's 26 rows plus cross-cutting's 8. It still says u7 and cross-cutting are unenumerated (`.agent/roadmap.md:167-179`) while the ledger has u7 26/26 and cross 8/24. Its dispatch table also shows no fixed check set for u1, u6 or u7 (`:158,163-164`), although all three committed sets appear in ledger coverage. Session-5's 31-mutant/current-gate paragraph is not labelled historical and is superseded by 45 mutants and 16/228.

Severity: med. Unit implementation statuses are DONE and calibration is sound, but the executable review resume point would dispatch completed u7 work and mis-size the remaining ledger. Acceptance check: add session 6 with u7=26 and cross=8 so total=354, mark cross 8/24 as the sole active surface, populate all seven check-set paths, record u3's missing gauge or state it unavailable, and label old session snapshots historical.

### X16

Parsed current-main polish into 29 entries: 29/29 contain `Accept:` and 29/29 contain `pri low|med|high`; MAIN's u7 fix closed the former register-wide field defect. Most remaining entries still describe open work.

One entry is already closed: `Four-query byte differential` says only category-A is byte-proven, but `tests/questions-live.test.ts:183-196` byte-compares all four bag-exported ids through `committedResult`. The roadmap cross-index has three unmatched `NOT verified` items: u2 dev+built browser smokes are now closed by `pnpm browser:check` but remain in roadmap; u5's missing separate red suite has no polish entry; u7's missing separate red suite has no polish entry. The u5 real-browser half maps to `Combobox predicates are jsdom-only`; u6 boot recovery, u7 walker and u7 axe each map correctly.

Severity: med. The register is structurally complete, but its sole consumer can spend a unit on already-shipped work while two explicitly deferred suites have no consumable unit. Acceptance check: remove the four-query entry; delete the now-closed u2 browser-smoke roadmap clause; add prioritized acceptance-bearing u5/u7 red-suite entries or strike each with a reason; then require every roadmap `NOT verified` clause to map one-to-one to an open register entry.

### X17

Sampled each contract at predicates touched by later review commits and compared it with current code plus ledger rulings. u2's protocol/clone surface still derives from `protocol.ts`; u3 now carries R35/R37/R45 scope notes; u4's catalog/service predicates still hold; u5 K2/K5 match their fixed tests; u6 V12 admits the ruled structural humanizer; u7 incorporates U7-05/U7-19/U7-20/U7-23-U7-26 and the eleven-step gate.

One contract remains stale. m1u1 P5.5 requires generated paths in both `.claude/settings.json` and Serena `ignored_paths`. Ledger U29 explicitly rejects that wording as superseded: gitignored `kb/generated` belongs only in deny because Serena's `ignore_all_files_in_gitignore` already excludes it. The contract was never amended and now contradicts project `CLAUDE.md` plus current synced-pair state.

Severity: low. Shipped behavior/configuration is correct, but a future acceptance run following the contract would introduce redundant/wrong Serena state. Acceptance check: rewrite P5.5 to partition gitignored generated paths into deny alone and tracked non-gitignored paths into both halves; then show current settings satisfy it.

### X18

Defined the M1-era range from `79233c3` (`M1 plan`) through current `main`: 42 commits. `git log --grep '(M1[. ]'` returns 35. A parser found every one of the 42 has `<scope>: <nonempty cause> → <nonempty fix>`; no summary-only subject exists.

Seven omit the trace and are invisible to the documented grep: `4befd92`, `d1cf0c4`, `6589470`, `e9bbfb6`, `3be2e54`, `b1b8816`, `25bc82b`. They are not unrelated history: they repair M1 probe SHAs, repo naming/sibling scanning, review modes, committed contracts, calibration/reporting and M1 authoring rules inside the M1 range.

Severity: low. History remains intelligible, but the roadmap's claimed trace query silently drops one sixth of the milestone commits. Acceptance check: record an inclusive range-based command as the authoritative M1 log, or rewrite/tag the seven commits so the documented trace query returns all 42; require cause/fix parsing to remain 42/42.

### X19

Censused 35 current-main `src/**`/`tools/**` code files. Production term rendering, guideline-id humanizing, state description, budget shaping and error formatting each have one semantic owner. `BUDGET_MAX` (validation ceiling) and `DEMO_BUDGET` (UI working value) must remain distinct. Canonical Prolog serialization and reader humanization must remain distinct. The balanced `result/1` parser duplicated in catalog/smoke is justified because the smoke's bag oracle must not share the artifact-under-test parser. Three validators' identical two-line failure printers are too small to fund a module.

One merge does serve all three bounds. `tools/smoke.mjs` and `tools/browser-check.mjs` each test/build `dist`, create a temporary nested root, copy `dist`, start the same shared server, collect identical broken-request checks, then close browser/server and remove the root. Current line census found five exact multi-file lines plus the same lifecycle around them. `tools/browser.mjs` already owns launch/serve plumbing, so keeping setup split lets a stale-build or cleanup fix drift between the two checks.

Severity: low. Both checks work, but browser harness maintenance is duplicated across u6 and post-review tooling. Acceptance check: extract one `withBuiltSite`/equivalent helper into `tools/browser.mjs`, leave each script only its scenario, and prove `pnpm smoke` plus `pnpm browser:check` retain their existing success and negative controls.

### X20

Sampled comments in all 35 current-main code files (file census plus targeted provenance scan). Non-obvious decisions are generally strong: worker/main deadline placement, macrotask cancellation, iterator closing, Prolog term ordering, null-prototype catalogs, font subset/range choices and nested-base browser serving all carry the constraint a fresh agent needs. I found no peculiar un-commented decision requiring a new explanation.

Provenance comments violate the rule at `src/questions/QuestionCombobox.svelte:3`; `tools/browser-check.mjs:2`; `tools/engine-check.mjs:1,26,136`; `tools/kb/check.mjs:40,51`; and `tools/presentation-check.mjs:2`. They cite unit/review ids, earlier broken patterns or a shipped-silently story instead of stating only the current constraint.

Pure-what headers occur at `src/questions/service.ts:1`, `serialize.ts:1`, `humanize.ts:1`, `src/demo/describe.ts:1`, `copy.ts:1`, `RunControls.svelte:2`, `AnswerPanel.svelte:2`, `src/engine/budget.ts:1`, `tools/kb/catalog.mjs:1` and `reproduce.mjs:1`. Later lines often add useful why, but these opening sentences themselves duplicate filenames/exports.

Severity: low. No correctness risk; the breach is recurring token cost and provenance drift. Acceptance check: delete the ten what-only headers, rewrite the eight provenance sites as timeless current constraints without row/history references, and retain the adjacent why comments byte-for-byte where possible.

### X21

Enumerated consumption-time surfaces. Human-facing UI = generated six question labels; `copy.ts` framing/instructions/disclosures/licence labels; `describe.ts` state/error/answer wording; inline `Answer`, `Choose an answer`, `Run`, `Cancel`, `Retry`; About/footer links; and externally supplied OFL texts. Current copy/presentation gates plus u7's fixed review cover sentence limits, banned filler, role labels, framing claims and descriptor output. The shipped prose is direct, active and terminology-stable; generated question text remains payload and legal licence text remains upstream text.

Machine/code surfaces are correctly separate: protocol `kind`/error codes, manifest/catalog JSON fields, request logs, diagnostics and validator failure codes are not rewritten as prose contracts. No UI string exposes those payload names except deliberately rendered error/limit codes.

Failure: the root tree contains no `README*`, docs directory or other reader entry document. The only Markdown outside `.agent/` is `CLAUDE.md`, which instructs agents rather than a user consuming the demo. Severity: med because the shipped app itself is readable, but a person landing on the repository receives neither purpose nor launch/use guidance. Acceptance check: add a human-facing README that states the demo's scope/limits and links to the run path, using ≤20-word instructions and ≤25-word descriptions.

### X22

Read current `package.json`, lock resolution and import/config sites. `swipl-wasm` is exact `8.0.7`; TypeScript `^5.9.3` cannot cross major 6 and stays under the `<6.1` cap; `@types/cytoscape` is absent; jsdom resolves exactly 29.1.1 and its `^29.1.1` range cannot cross to incompatible 30. Runtime font packages and swipl live in dependencies; linters, compilers, tests and DOM tooling live in devDependencies. Package-manager deprecation fields were empty.

`pnpm outdated --format json` reported six: `@types/node` 26.4.0→26.4.1, `globals` 17.11.0→17.12.0, `typescript-eslint` 8.68.0→8.69.0 (compatible maintenance drift); jsdom 29.1.1→30.0.1 (held for Node incompatibility); swipl-wasm 8.0.7→8.1.1 (held exact by R26); TypeScript 5.9.3→7.0.2 (held by the supported cap). None is deprecated.

Failure: `cytoscape ^3.34.2` has no import or tool/test use (`git grep` rc 1 with an axe import as positive control). M3 is unplanned, so it is installed today only for future scope. Severity: low; tree-shaking keeps it out of `dist`, but install surface and dependency policy are needlessly wider. Acceptance check: remove `cytoscape` from package and lock until M3 starts, run the gate, and separately review the three compatible outdated dev-tool updates.

### X23

Walked every committed top-level file as a fresh user. There is no README/docs setup path. `package.json` exposes scripts but declares no `packageManager`, `engines`, Node version, pnpm prerequisite, dependency-install command, generated-KB step or recommended dev/preview command. `CLAUDE.md` is agent policy and `.agent/**` is internal project state, not a user setup contract.

Therefore no clean-clone execution was possible **as written**: the first step is absent before any command can be tested. This review's successful builds/tests relied on the machine's pre-existing pnpm, Node and shared `node_modules` symlink, which a clean clone does not have.

Severity: med. A knowledgeable contributor can infer `pnpm install`, but X23 requires only committed instructions and every prerequisite stated. Acceptance check: commit a README plus `packageManager`/`engines`; from a dependency-free clone follow only that path through `pnpm install --frozen-lockfile`, generated-asset build, `pnpm dev` (or preview), and one stated verification command, with no unstated global tool except the declared Node/package-manager pair.

### X24

Enumerated durable scratch/branch evidence. Good pairs: mutation claim → `.scratch/verify-fixes.py` command + `Mutation harness is scratch-local` port entry; report grading → `.scratch/validate-report.py` command + validator-port acceptance inside `Report validator splits on escaped pipes`; u1 partial reports → named files + `Finish the u1 wave-1 reports`; visual-QA evidence → `wt/map-m1u7` copy/run recipe + typed-port polish entry. Both scratch scripts and all named branches exist. Seed/build-ledger scripts no longer back a claim because the committed hand-maintained ledger is explicitly authoritative.

Failure: `tools/probe-u3.mjs` on `wt/rev-m1u3-4` backs durable R38/R39/R41/R42/R45 browser claims. Memory gives a copy/run command but no scheduled committed port; the R45 product-fix entry does not port the evidence harness. Likewise `wt/res-m1-1` and `wt/res-m1-2` back stack/tooling/dependency measurements but carry only branch summaries, with neither per-probe regeneration commands nor a port schedule.

Severity: med. Branches currently survive, so evidence is recoverable, but deletion makes seven claim families non-rerunnable from committed state. Acceptance check: commit or schedule one typed browser harness covering R38/R39/R41/R42/R45, and either port the still-authoritative `res-m1-*` probes with commands or prune every durable claim that relies on them. The report validator run at review close is the required live recorded-path check.

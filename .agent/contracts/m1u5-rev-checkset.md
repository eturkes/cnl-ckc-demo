# rev-m1u5 — fixed check set for M1.u5, accessible question intake

28 rows. Sources = `.agent/contracts/m1u5.md` (D1–D7, S/K/P/B predicates, invariants,
verdict table), `.agent/contracts/m1u5-matrix.md`, `.agent/roadmap.md` u5,
`.agent/memory.md` "Question intake (u5)", `.agent/polish.md` u5 entries, `CLAUDE.md`.

Review target = `70c49b5`. u5 shipped `src/questions/QuestionCombobox.svelte` +
`tests/question-combobox.dom.test.ts` + the `vite.config.ts` node/dom projects +
`package.json` (`--fail-on-warnings`, `jsdom`, `axe-core`) + an `App.svelte` intake
against local `$state`. u6 then rewrote `App.svelte` so selection lives in
`DemoController`, and u7 rewrote it again. Adjudicate u5's predicates against the
surface AS COMMITTED at the target, and treat a u5 guarantee that u6 or u7 broke as a
u5 row `fail`.

The roadmap records two u5 gaps up front, so neither is a finding by itself: there is no
`test-m1u5` red suite (D6) and no real-browser run — every predicate is jsdom-observed.
What IS a finding: a predicate whose jsdom observation does not hold in a browser, or a
suite case that would stay green against a broken widget.

Ids backticked so a grader reads one row per id from a report's verdict table alone.

| id | check | source | how to decide it |
| --- | --- | --- | --- |
| `I01` | mechanical · Does the focusable host carry `role="combobox"`, `aria-labelledby` pointing at its visible label, and `aria-controls` equal to the listbox `id` in BOTH states, with no `aria-haspopup` attribute at all? | S1, S3, S4 | Render closed and open; read the four attributes each time; compute the accessible name and assert it is the label text, not the selected question. |
| `I02` | mechanical · Is `aria-expanded` exactly `"false"` while the listbox is absent and `"true"` while it is rendered, across open, commit, cancel and toggle paths? | S2 | Drive false → true → false through each of the four paths and read the attribute after every transition. |
| `I03` | mechanical · Is the popup `role="listbox"`, named by the same label, owning exactly six `role="option"` children in `QUESTION_IDS` order, with every label read from `QUESTION_CATALOG` rather than hard-coded? | S5, Corpus | Read the six option texts and ids in DOM order against `QUESTION_IDS`; scan the component and the suite for any literal question sentence. |
| `I04` | mechanical · With selection `q`, does option `q` carry `aria-selected="true"` and the other five `"false"` — present on all six rather than omitted on the unselected? | S6 | Render each of the six selections plus `null` and census the attribute on every option. |
| `I05` | mechanical · Open: does `aria-activedescendant` equal the active option's `id`; closed: is the attribute ABSENT (not empty); and is `document.activeElement` the combobox in both states? | S7 | Assert presence and value open, absence closed, and read `document.activeElement` after every keyboard and pointer transition. |
| `I06` | mechanical · Does the combobox's own text render the selected question, or the prompt when `selected` is null, with no focusable descendant anywhere in the widget? | S8 | Render both states; walk the subtree for `tabindex >= 0`, `<button>`, `<a href>`, `<input>` and any natively focusable node. |
| `I07` | mechanical · Closed keys: do `ArrowDown` and `Alt+ArrowDown` open with the active option at the current selection, does `ArrowUp` open with option 1 active, is `Alt+ArrowUp` a no-op, and does none of them emit `onSelect`? | K1, K2, D3 | Drive all four keys from a selected and an unselected start; assert the open state, the active id and an emit count of 0. |
| `I08` | mechanical · Closed keys: do `Enter` and `Space` open without emitting, and do `Home` and `End` open with option 1 and option 6 active? | K3, K4 | Drive each key from closed; assert open state, active id and zero emits. |
| `I09` | mechanical · Typeahead: does a printable character open and activate the first question whose text starts with the typed prefix case-insensitively, does the buffer expire after 500 ms, and does a repeated single character cycle matches? | K5, D3 | Drive multi-character prefixes, a repeat cycle and an expiry using fake timers; assert the active option each time; check whether the prefix is matched against catalog text rather than an id. |
| `I10` | mechanical · Open arrows: do `ArrowDown` and `ArrowUp` move the active index by one and CLAMP at option 6 and option 1 without wrapping, and do `Home` and `End` activate option 1 and option 6, all without emitting? | K6, K7 | Walk to both ends and press once more; assert no wrap, no emit and the exact active id. |
| `I11` | mechanical · Does `Enter` while open emit `onSelect(active)` exactly once, close the listbox, and leave `document.activeElement` on the combobox? | K8 | Count emits across the transition; assert the argument, the closed state and focus. |
| `I12` | mechanical · Does `Escape` close, emit nothing, and RESET the active option to the controlled selection rather than leaving the last active one? | K9 | Move the active option away from the selection, press Escape, reopen and read the active id. |
| `I13` | mechanical · Does `Tab` emit `onSelect(active)` exactly once, close, and leave `defaultPrevented === false` so focus still moves? | K10 | Dispatch a cancelable `Tab` event; assert emit count, closed state and `defaultPrevented`. |
| `I14` | mechanical · Does `Alt+ArrowUp` while open emit `onSelect(active)` exactly once and close, per the D3 ruling that splits it from the closed-state no-op? | K11, D3 | Drive it open with the active option moved off the selection; assert the emitted id and the closed state. |
| `I15` | mechanical · Does a click on the combobox toggle the listbox without emitting, and does a `focusout` to a node outside the widget close it without emitting? | P1, P3 | Toggle twice and assert zero emits; move focus to an outside node and assert the widget closed and emitted nothing. |
| `I16` | mechanical · Does a click on option `q` emit `onSelect(q)` exactly once, close, and return focus to the combobox — including a click that lands on a child node of the option rather than the option itself? | P2 | Click the option and then an inner node; assert one emit each, the argument, the closed state and focus. |
| `I17` | mechanical · Does every active-option change call `scrollIntoView({ block: 'nearest' })` on that option and on no other, once per change? | B1 | Spy `scrollIntoView` across keyboard, typeahead and open transitions; assert call count, receiver and argument. |
| `I18` | mechanical · Is every `onSelect` argument a member of `QUESTION_IDS`, with no key sequence producing free text or an unknown id, and does the component hold no selection state — a fixed `selected` prop plus a commit leaves the DOM unchanged until the parent passes a new prop? | B2, D7, Invariants | Fuzz a key sequence including printable characters, arrows and commits; assert every emitted argument; render with a frozen prop and assert the displayed text after a commit. |
| `I19` | mechanical · Does `axe.run` over the widget report zero violations closed AND open, with `color-contrast` the only permitted `incomplete` id? | B3, `.agent/memory.md` axe-core jsdom note | Run the sweep in both states at the target commit and enumerate every `incomplete` id, not just the violations. |
| `I20` | mechanical · Is `pnpm check` 0 errors and 0 warnings under `--fail-on-warnings`, and is D4's rationale still true — does `eslint-plugin-svelte` still ship zero `a11y-*` rules, so the compiler is the only a11y linter in the gate? | B4, D4 | Run the gate step and record counts; enumerate the installed plugin's rule ids and count the `a11y` ones; confirm the flag is in the committed script. |
| `I21` | mechanical · Is the component a controlled leaf — props exactly `{ selected: QuestionId \| null; onSelect: (id: QuestionId) => void }`, importing from `./catalog.js` alone, with no `../engine/` import, no `AnswerService` and no worker anywhere in its transitive graph? | D7, Invariants | Read the prop declaration; walk the transitive import graph from the component; scan for engine and service identifiers. |
| `I22` | mechanical · Is the host a `<div role="combobox" tabindex="0">` rather than a readonly `<input>` or a `<button>`, and does the widget still read as a text field visually through CSS alone, so D1's free-text swap stays a host swap? | D1 | Read the rendered tag and attributes; read the styling that carries the text-field affordance; check no behaviour depends on the div-ness beyond the host. |
| `I23` | mechanical · Do the three D3 keyboard divergences hold as ruled — closed `Alt+ArrowUp` inert, 500 ms case-insensitive typeahead with single-character cycling, open `Alt+ArrowUp` commits — and does the committed suite assert each ruling rather than the APG default? | D3 | Read each ruling against its `it` case and against the widget; report any place the code and the ruling agree but the suite does not assert it. |
| `I24` | mechanical · Does the vitest split hold — `tests/**/*.dom.test.ts` under jsdom with `resolve.conditions: ['browser']`, the node project excluding that glob so `swipl-wasm` keeps its node entry, and jsdom pinned to 29.1.1? | D5, `.agent/memory.md` jsdom pin | Read `vite.config.ts` and `package.json`; run each project alone; confirm the node project still resolves the engine and the dom project still mounts Svelte. |
| `I25` | mechanical · Is the `svelte-ignore` exactly one, scoped to the listbox click handler, and is its justification true — does the keyboard path really live on the combobox so no keyboard user needs the listbox handler? | Register, `.agent/memory.md` u5 compiler note | Count and locate every suppression comment in the component; delete it and confirm the warning it silences is the named one; verify keyboard commit works with pointer events removed. |
| `I26` | judgment · Which of the 26 predicates are jsdom artefacts — where would a real browser diverge (focus management, `scrollIntoView`, typeahead timing, `focusout` ordering, `aria-activedescendant` announcement)? | `.agent/roadmap.md` u5 "NOT verified", D6 | Rule each predicate browser-safe or browser-suspect with a reason; name the smallest browser probe that would close the suspect set; do NOT run a browser — report the probe, since the shared browser stays with MAIN this session. |
| `I27` | mechanical · Is the committed suite BOUND to the widget's behaviour — does each of five mutations turn `pnpm exec vitest run --project dom` red: drop `aria-activedescendant`, wrap instead of clamp at the ends, emit on `Escape`, skip `scrollIntoView`, and reverse the option order? | D6, `CLAUDE.md` Engineering "deterministic checks own every rule a tool can decide" | Apply each mutation in your worktree, run the dom project, record rc and the failing case name, and RESTORE the file after each. A mutation that leaves the suite green is a `fail` naming the unbound predicate. |
| `I28` | judgment · Does every touched durable file pass a per-file `CLAUDE.md` ledger, and does the widget's own code meet the Engineering bar — one module, no duplicated state machine, why-only comments, ASD-STE100 for the prompt and label copy? | `CLAUDE.md` Authoring + Engineering, D7 | Adjudicate `QuestionCombobox.svelte`, `tests/question-combobox.dom.test.ts`, `vite.config.ts`, `.agent/contracts/m1u5.md` and the u5 memory bullets file by file; report each breach with a `file:line` and a concrete acceptance check. |

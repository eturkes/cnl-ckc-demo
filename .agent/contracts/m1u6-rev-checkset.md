# rev-m1u6 — fixed check set for M1.u6, run lifecycle + answer states

28 rows. Sources = `.agent/contracts/m1u6.md` (D1–D10, C/R/V/G predicates, fixed
public API, Q1–Q8 rulings, verdict table), `.agent/roadmap.md` u6, `.agent/memory.md`
"Run lifecycle and answer states (u6)", `.agent/polish.md` u6 entries, `CLAUDE.md`.

Review target = `6ff2d9c`. u6 shipped
`src/demo/{DemoController.svelte.ts,describe.ts,RunControls.svelte,AnswerPanel.svelte}`
+ rewritten `src/App.svelte` + `tools/smoke.mjs` + `tests/{engine-cancel,demo-lifecycle,demo-controller.dom}.test.ts`.
u7 then rewrote `App.svelte` and `AnswerPanel.svelte` and added `src/demo/{copy,descriptor}.ts`
and `AboutPanel.svelte`; adjudicate u6's predicates against the surface AS COMMITTED at
the target, and treat a u6 guarantee that u7 broke as a u6 row `fail`.

Four predicates are recorded `pass after fix` in the contract's verdict table — R3, R4,
V6, V10. Re-derive each independently; a fix the diff-blind suite found is where a
second defect most likely hides.

Ids backticked so a grader reads one row per id from a report's verdict table alone.

| id | check | source | how to decide it |
| --- | --- | --- | --- |
| `L01` | mechanical · Is the cancellation surface a TRAILING OPTIONAL `AbortSignal` on `EngineClient.query` and `AnswerService.ask`, with every existing two-argument call unchanged, and does the id-only guarantee hold so free text, an unknown id and a raw goal each return `rejected` and post zero engine requests? | C1, C2, D1 | Read both signatures and every call site; drive all three rejection inputs through a spy-backed service and count posted requests. |
| `L02` | mechanical · Does aborting a live query settle that caller `cancelled` carrying the solutions already proven, and does one abort post exactly one `cancel` targeting that query's id while a second abort on the same signal posts none? | C3, C4 | Abort mid-run with proven solutions and assert both the outcome kind and the retained solutions; count `cancel` posts across two aborts on one signal. |
| `L03` | mechanical · Does a pre-aborted signal settle `cancelled` without spawning a worker and without posting a query, and does settlement remove the abort listener so a later abort posts nothing and raises no unhandled rejection? | C5, C6 | Pass an already-aborted signal and count worker spawns and posts; abort after settle and assert zero posts plus no unhandled-rejection event. |
| `L04` | mechanical · Does a hard reset during signal-bound live queries settle every caller exactly once, with no late response from the retired generation claiming a request? | C7, `.agent/memory.md` u3 hard cancel | Run two signal-bound queries, reset, and assert one settlement each; replay a retired-generation response and assert it is dropped. |
| `L05` | mechanical · Does the controller own exactly one `EngineClient` and one `AnswerService`, and does a boot failure become the typed `boot-error` state rather than a rejected promise reaching a template? | R1, fixed public API | Read `createDemoEngine` and the constructor; inject a failing boot and assert the state kind plus the absence of an unhandled rejection. |
| `L06` | mechanical · Does selecting a question record the selection and start no run, and is every disabled-state public call a state-preserving no-op — `run()` while booting, `cancel()` with nothing live, `selectSolution` out of range? | R2, Q2, Q3, Q8 | Drive each illegal call from each state and assert byte-identical state before and after; confirm out-of-range `selectSolution` neither clamps nor throws. |
| `L07` | mechanical · Does `run()` enter `running` SYNCHRONOUSLY and dispatch its engine call in its own tick when nothing is live, then reach `settled` carrying the `AnswerResult`? | R3 (`pass after fix`), `.agent/memory.md` "the engine call must go out in `run()`'s own tick" | Read the await ordering for any unconditional `await previous?.done`; assert the state kind before any microtask drain and assert the engine call was posted in the same tick. |
| `L08` | mechanical · Does starting a run while one is live abort the predecessor, become visible immediately, and issue the new engine call only after the predecessor settles, holding overlapping engine queries at 0? | R4 (`pass after fix`), D2, `.agent/memory.md` "chains on the ENGINE CALL, not on the state write" | Instrument concurrent asks and assert a maximum of 1; assert the successor's visible state changes before its engine call; confirm the chain is on `ActiveRun.query`, not on the settle promise. |
| `L09` | mechanical · Is every controller state write gated on the run's own `AbortController` still being active, so a retired run's late result never replaces the active run's state? | R5, D3 | Census the state writes for the identity guard; force a retired run to resolve after a successor settles and assert the successor's state survives. |
| `L10` | mechanical · Does `cancel()` abort the active run, enter `cancelling`, and await that run's settlement, with `settled` always the terminal state? | R6, fixed public API "`cancelling` is transient" | Assert the intermediate kind, then that the promise resolves only after settlement; confirm no path leaves `cancelling` terminal. |
| `L11` | mechanical · Does `retry()` rerun `state.id` rather than `selected`, under a FRESH `AbortController` that is never a reused aborted signal? | R7, D4, Q4 | Select a different id than the one that ran, retry, and assert the id that runs; assert a new controller instance and a nonzero post count. |
| `L12` | mechanical · Does `dispose()` abort the active run and dispose the client, with no state write following? | R8 | Dispose mid-run; assert one dispose call, an aborted signal, and a state snapshot unchanged after the in-flight run resolves. |
| `L13` | mechanical · Is `DEMO_BUDGET` frozen, corpus-measured and the only budget the controller passes, with the controller exposing neither a goal nor `consult`, and is `wallClockMs` 5000 rather than the probe's 2000? | R9, D5 | `Object.isFrozen` plus identity comparison at the call site; census the controller's public surface; read the value and confirm the contract's rationale still matches the shipped number. |
| `L14` | mechanical · Do all six catalog ids settle `answer` under `DEMO_BUDGET` against the real generated PVM at counts 7 / 5 / 2 / 1 / 3 / 12? | R10, Q corpus | Run the live lifecycle suite; read each count from the run rather than from a fixture; cross-check against `.agent/memory.md`'s live counts. |
| `L15` | mechanical · Is there exactly one `role="status"` node and exactly one `role="alert"` node, both mounted from FIRST RENDER, with error text appearing in the alert alone and never mirrored into the status node, and is the status node never focused? | V1, V2, D6 | Query both roles at first render and in all ten states; assert counts of 1; assert the error string appears once in the accessibility tree. |
| `L16` | mechanical · Do the ten states each render their own visible text label — `booting`, `boot-error`, `idle`, `running`, `cancelling`, and `settled` split by `result.kind` into `answer`, `failure`, `limit`, `cancelled`, `error` — with none distinguished by colour or icon alone? | V3, Q6 | Drive each state and capture the rendered text; assert pairwise distinctness; confirm no state's only differentiator is a class or a glyph. |
| `L17` | mechanical · Does `limit` name its `LimitKind`, label retained solutions as partial, and never read as "no answers" merely because none were retained? | V4 | Render all six `LimitKind` values with and without retained rows; assert the kind name appears and the partial labelling is present. |
| `L18` | mechanical · Does `cancelled` read as cancelled and report its partial solution count separately from the answer count? | V5 | Render cancelled with 0, 1 and N retained solutions; assert wording and a distinct count surface. |
| `L19` | mechanical · Does `failure` read as no proof found, and does an existential question render `yes` or `no` rather than an empty row set or unlabelled radios? | V6 (`pass after fix`), `.agent/memory.md` "an existential question projects no columns" | Render existential `failure` and existential `answer` with 12 solutions; assert the "no proof" wording and that `answerRows` returns `[]` for an empty projection. |
| `L20` | mechanical · Do N>1 solutions render as one native radio group inside `<fieldset><legend>` with a single tab stop, N=1 render the sole answer without a choice group, and N=0 render the terminal state alone, with `solutionIndex` reset to `-1` per run and set to 0 when solutions arrive? | V7, D7, Q5, Q8 | Render each cardinality; count tab stops and `fieldset`/`legend`; assert `solutionIndex` across a run boundary. |
| `L21` | mechanical · Are Run, Cancel and Retry native `<button type="button">` using native `disabled`, with Run disabled without a selection and during a live run, Cancel enabled only during a live run and disabled while booting, and Retry rendered only for `settled` with `result.kind === 'error'`? | V8, D8, Q7 | Build the full state × control enablement matrix and compare against the predicate; assert no `aria-disabled` substitute. |
| `L22` | mechanical · Does focus stay put on run start, completion and cancellation, moving only when the control holding it becomes disabled or absent — to Retry after an error and to Run otherwise? | V9, D9, `.agent/memory.md` "$effect.pre" reading | Track `document.activeElement` across every transition; assert the handoff is read before the disable and acted on after. |
| `L23` | mechanical · Does the answer region stay MOUNTED in every state and carry `aria-busy="true"` only while a run is live — `running` and `cancelling` — and `false` otherwise, including during `booting`? | V10 (`pass after fix`), D6 | Assert the region's presence in all ten states and read `aria-busy` in each; confirm `booting` is not treated as busy. |
| `L24` | mechanical · Does `axe.run` report zero violations in every state, with every `incomplete` id except `color-contrast` absent? | V11, `.agent/memory.md` axe-core jsdom note, `.agent/polish.md` "Axe over the two new disclosures" | Run the axe sweep across all ten states at the target commit; enumerate the `incomplete` ids; report whether the sweep covers the u7 About panel and the canonical-answer `<details>`. |
| `L25` | mechanical · Does rendered answer text come only from `serialized` or `solution.display[...]`, with no binding JS-templated or `JSON.stringify`ed? | V12, u2 P3.12 | Read every render site; feed a poison binding whose native form differs from its display text and assert the display text is what surfaces. |
| `L26` | mechanical · Is `pnpm gate` rc 0 from a clean `kb/generated`, and does `pnpm check` report 0 errors and 0 warnings under `--fail-on-warnings`? | G1, G2 | Run both from the target commit and record rc, file count, test count and asset count. |
| `L27` | mechanical · Does the nested-path browser smoke pass — build, copy `dist` under a nested path, serve it, and reach `[data-engine="ready"]` with no `data-engine="error"` and 200 for the nested index, CSS, main JS, worker and PVM — and does its negative control actually exercise the build path? | G3, `.agent/memory.md` smoke bullet, `.agent/polish.md` "Smoke negative control is weak" | Run `pnpm smoke` and read the request log for the five asset classes; then run the recorded negative control and rule on what it proves and what it does not. |
| `L28` | judgment · Does the shipped layout match D10 and the fixed public API exactly, does the Q1 injection seam hold so `App` takes `controller?: DemoController` and `RunControls`/`AnswerPanel` never see the controller, and does every touched durable file pass a per-file `CLAUDE.md` ledger? | D10, Q1, fixed public API, `CLAUDE.md` Authoring + Engineering | Signature-by-signature comparison against the declared API; read the prop surfaces of both child components; adjudicate the ledger — dense symbol-forward text, no provenance narrative, why-only comments, deduplicated modules, ASD-STE100 for human-facing copy, deterministic checks for mechanical rules. |

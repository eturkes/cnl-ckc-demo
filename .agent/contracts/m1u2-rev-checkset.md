# rev-m1u2 — fixed check set for M1.u2, Prolog engine worker

28 rows. Sources = `.agent/contracts/m1u2.md` (D0, D1, C1, P1–P6, I, G, Q),
`.agent/roadmap.md` u2, `.agent/memory.md` "Engine runtime (u2)" + "Generated
runtime payload (u1)", `.agent/polish.md` u2 entries, `CLAUDE.md`.

Review target = `6ff2d9c`. u2 shipped `src/engine/{protocol,terms,session,client,worker}.ts`
+ `tests/engine-session.test.ts`; sessions 1–3 landed M1-review fixes in the same
tree, so adjudicate the surface AS COMMITTED at the target, not the u2 commit alone.
C1's preamble declares its paths provisional, so a path divergence is not a defect —
a missing CONCERN is.

Ids backticked so a grader reads one row per id from a report's verdict table alone.

| id | check | source | how to decide it |
| --- | --- | --- | --- |
| `E01` | mechanical · Does the engine live only inside the worker, with no main-thread module importing `swipl-wasm` transitively, and does the built main chunk carry zero engine bytes? | P1.1, I "main-thread chunk contains no engine bytes", D0.1 | Walk the import graph from `src/main.ts`; then build and scan the emitted main chunk for engine markers against the worker chunk as positive control. |
| `E02` | mechanical · Does the worker load the saved PVM through a Vite `?url` import plus `fetch`, and never consult the 337 source documents at runtime? | P1.2, D0.3 | Read the import site and the loader; scan for any runtime consult of `pl/` payload; confirm the build emits a hashed 437132-byte PVM with no `publicDir` copy and no base64 inlining. |
| `E03` | mechanical · Are schema version and document count READ from the booted engine and compared against `kb-manifest.json`, with a mismatch a typed failure, and do 337 and schema 1 appear in production code only as read values? | P1.3, D0.5, I "never literal pass conditions" | Read the boot path; scan `src/` for the literals and rule on every hit; force a manifest/engine mismatch and observe the typed failure rather than a warning. |
| `E04` | mechanical · Does boot failure — fetch failure, malformed image, engine init throw — surface as a typed terminal state without hanging a pending request and without an unhandled rejection? | P1.4 | Drive each of the three failure modes through the session with an injected loader; assert a typed terminal response, zero pending requests, and no unhandled-rejection event. |
| `E05` | mechanical · Does exactly one engine boot serve every subsequent query in a session, so a second query never re-boots? | P1.5 | Count loader invocations across two sequential queries and across two concurrent ones; confirm one boot and one shared engine. |
| `E06` | mechanical · Does every payload crossing the boundary survive `structuredClone` unchanged, with no class instance, function or `Error` object crossing? | P2.1, D0.4 | `structuredClone` every request and response DTO the protocol declares, including every error shape; assert deep equality and reject any prototype other than plain object/array. |
| `E07` | mechanical · Does every request carry a client-generated id that every response echoes, and is an unmatched response a typed protocol violation rather than a silent drop? | P2.2 | Read the correlation path; post a response with an unknown id and one with a duplicate id; assert a typed violation in each case, never a swallow. |
| `E08` | mechanical · Does every request terminate in exactly one terminal outcome, with solutions delivered as one correlated batch, and is a malformed goal distinguishable from an honest zero-solution run? | P2.3, D1.2, `.agent/memory.md` "a malformed goal yields NO solution rather than raising" | Census the terminal responses per request over the Q protocol cases; assert no request stays pending and none settles twice; run a malformed goal and a genuinely zero-solution goal and require different outcomes. |
| `E09` | mechanical · Are request and response types declared once and imported by both sides, with neither side restating the shape? | P2.4 | Read `protocol.ts` and both consumers; scan for any locally redeclared request/response interface or structural duplicate. |
| `E10` | mechanical · Does a worker-level failure — `onerror`, `messageerror`, in-worker unhandled rejection — reject every in-flight request with a typed failure rather than leaving them pending? | P2.5 | Drive each of the three failure channels with two in-flight requests; assert both settle typed, and that timers and listeners are released. |
| `E11` | mechanical · Does the decoder produce a discriminated union whose exhaustiveness the compiler enforces, so a new variant fails `pnpm check`? | P3.1 | Read the union and its switch sites; inject a new variant and require `pnpm check` rc 1 naming a `never` assignment, then revert. |
| `E12` | mechanical · Do `foo(bar,7)` and `foo([bar,7])` decode to different values, preserving the argument-vector versus single-list-argument distinction? | P3.2, Q corpus | Run both through the real decoder; assert distinct shapes and distinct display text; confirm the ONE-ELEMENT envelope quirk at `value[value.functor][0]` is handled rather than assumed. |
| `E13` | mechanical · Do rationals round-trip with orientation intact, so `1r3` never decodes to or renders as `3r1`? | P3.3, `.agent/memory.md` JSON corruption path | Decode `1r3` and `3r1` from the live engine; assert numerator/denominator and rendered text for each. |
| `E14` | mechanical · Do an atom and a Prolog string with identical text decode to distinguishable values, and does an unbound variable decode to an explicit variable rather than `undefined`, `null` or a fabricated atom, including a variable shared across two arguments? | P3.4, P3.6, Q corpus | Live-engine cases for each; assert tag distinctness for atom/string and identity preservation for the shared variable. |
| `E15` | mechanical · Is an integer outside the IEEE-754 safe range decoded without loss, and is the integral-float deviation (`1.0` arriving as `integer`) declared rather than silent? | P3.5, `.agent/memory.md` "Integral floats decode as integers", `.agent/polish.md` | Decode an integer above `2^53` and assert exactness; decode `1.0` and check the code or contract states the deviation at the decode site. |
| `E16` | mechanical · Is decode → encode → re-query an identity over every shape the KB produces, and does `'$guideline_id'(product,'cdc2022-opioid-rec02',1,ref(1),[])` round-trip with 5 arguments, quoted `$`-functor, nested `ref(1)` and a genuine empty list? | P3.7, P3.8, I "term fixtures synthesized or read from the live engine" | Run the round-trip battery against the real PVM; assert arity and each argument shape individually; confirm no committed expected-answer blob supplies the fixture. |
| `E17` | judgment · Does decoding happen inside the worker before any DTO is built, does no native engine value re-enter a query, and is re-encoding done only through the engine's own `Compound`/`List`/`Rational`/`String`/`Var` constructors? | P3.9, P3.10, D1.1 | Read the worker's decode ordering and every encode site; assert no native value is stored, forwarded or re-entered; confirm the constructors are the only construction path. |
| `E18` | mechanical · Does an unrecognized or unsupported binding tag fail closed with a typed decode error, never coerced to a string, dropped or passed through? | P3.11 | Feed a synthetic value carrying an unknown `$t` tag and a `$tag` dict; assert a typed decode error in each case. |
| `E19` | mechanical · Does production code never `JSON.stringify` an engine value, and is that enforced by more than convention? | P3.12, `.agent/memory.md` "the one measured corruption path" | Scan every production module for `JSON.stringify` and rule on each hit; check whether any gate step or type barrier prevents a new one. |
| `E20` | mechanical · Does display text come from `term_string/3` with `quoted(true), numbervars(true), ignore_ops(true)`, re-read as the same term, quote `$`-prefixed, hyphenated and mixed-case atoms correctly, and stay derived rather than assembled from a per-predicate template? | P4.1, P4.2, P4.3, D1.3 | Read the display path for any per-functor branch; render the Q corpus and re-read each string through the engine, asserting term equality; include `''`, an atom containing a quote and an operator term. |
| `E21` | mechanical · Does a committed test boot the real generated `kb.pvm` and prove 337 documents at schema 1, reading no fixture standing in for engine output? | P5.1, G2 | Run the live suite; trace each expectation to the engine or the manifest; flag any constant the same test computed from the artifact it verifies. |
| `E22` | mechanical · Does a committed test execute a real KB goal end to end through the shipped client surface and prove the 7 category-A solutions? | P5.2, G2 | Run it; confirm the call path goes through the shipped client rather than an internal helper, and that the count comes from the run. |
| `E23` | mechanical · Does the anti-hard-coding probe inject a `dynamic/1`-declared overlay clause, re-run, and assert the result CHANGES, so a stubbed engine cannot pass it? | P5.3, `.agent/memory.md` anti-hard-coding recipe | Read the probe; stub the engine and require the test to fail; confirm the overlay is declared dynamic first, since the schema predicates are static. |
| `E24` | mechanical · Does no production module import from `queries/answers` transitively, and does the sibling-path scan for `../cnl-ckc` stay empty over the paths it scopes? | P5.4, P6.4 | Run `pnpm kb:asset-check`; plant one positive control per reach class and require nonzero; confirm the scan does not trip on this repo's own `cnl-ckc-demo` prefix. |
| `E25` | mechanical · Do the live tests FAIL rather than skip when `kb/generated/` is absent? | P5.5 | Remove `kb/generated`, run `pnpm test`, and require nonzero with zero skipped live cases; restore afterwards. |
| `E26` | mechanical · Is `pnpm gate` rc 0 from a clean checkout with `kb/generated/` deleted, are every new generated and cache path in `.gitignore` and in the synced read-exclusion pair, and what is the current standing of the P6.2 dev-server-and-built-output browser proof? | P6.1, P6.2, P6.3, G1, G3, `.agent/polish.md` "u2 browser smokes" | Run G1 and record rc plus counts; read `.gitignore`, `.claude/settings.json` `permissions.deny` and `.serena/project.yml` `ignored_paths` against the emitted paths, applying the project rule that regenerable gitignored caches go to `permissions.deny` alone; then state exactly what evidence exists for P6.2 today and what it does not cover. |
| `E27` | judgment · Does the committed suite cover the Q corpus — 19 term shapes and 8 protocol cases — with fixtures synthesized or read from the live engine, and is the worker's message handler reachable as a pure function a test can drive without a DOM `Worker`? | Q corpus, I "message handler reachable as a pure function", `.agent/polish.md` "u2 red suite" | Census `tests/engine-session.test.ts` case names against the Q list and report the gaps by name; read the worker/session split and drive the handler directly. |
| `E28` | judgment · Do the C1 concerns all ship somewhere, does D0.2's engine split hold in the shipped graph (`swipl-bundle-no-data` only, no `swipl-bundle` bytes), and does every touched durable file pass a per-file `CLAUDE.md` ledger? | C1, D0.1, D0.2, `CLAUDE.md` Authoring + Engineering | Concern census against the shipped modules; scan the build output for `swipl-bundle` bytes; adjudicate the ledger — dense symbol-forward text, no provenance narrative, why-only comments, deduplicated modules, deterministic checks for mechanical rules. |

# rev-m1u4 — adversarial review of u4

Phase 1 = enumerate the check set (24 rows, batch size 6). Phase 2 = adjudicate against MAIN diff.

Grade: `python3 -P .scratch/validate-report.py .scratch/agents/rev-m1u4.md`

| id | finding | evidence |
| --- | --- | --- |
| c01 | Does the catalog expose exactly six stable string IDs, resolve each valid ID through its supported lookup path, and keep prototype-like names unreachable? [P1.1] Close: key census plus computed-access and `__proto__`/`constructor` negatives. | `.scratch/contract-m1u4.md` P1.1 and Q Catalog |
| c02 | Do unknown, empty, whitespace, ACE, raw-Prolog, goal-shaped, case-shifted and punctuated strings reject with the same typed error before any engine call? [P1.2–P1.3] Close: spy-backed Q-corpus table. | `.scratch/contract-m1u4.md` P1.2–P1.3 and Q Catalog |
| c03 | Does every entry ship its compiled goal, explicit projection and provenance, with none derived at call time from display or humanized text? [P1.4,P1.6] Close: six-entry structural assertion plus call-path review. | `.scratch/contract-m1u4.md` P1.4, P1.6; `.agent/roadmap.md` u4 |
| c04 | Are all four exported goals generated during `kb:build` from verified in-memory bag bytes, byte-equal to each bag `goal/1` argument in canonical prefix form, with no extraction or transcription path? [P1.5,P1.7,D1,D1.1,D4] Close: bag-at-test-time byte comparison plus source-path scan. | `.scratch/contract-m1u4.md` D1, D1.1, D4, P1.5, P1.7; `.agent/memory.md` Knowledge base |
| c05 | Does one shared token-aware derivation replace exactly one quoted atom per authored goal, reject zero/two/substring near-misses, and leave both authored IDs live with counts 5 and 3? [P1.8,D2,D3] Close: substitution mutation table plus real-PVM execution. | `.scratch/contract-m1u4.md` D2, D3, P1.8 and Q Generation |
| c06 | Is catalog generation byte-deterministic and fail-closed for mutated, missing, extra or structurally unexpected query clauses? [P1.9–P1.10] Close: two forced-build byte comparison plus the Q mutation suite, with every malformed case making `pnpm kb:build` nonzero. | `.scratch/contract-m1u4.md` P1.9–P1.10 and Q Generation |
| c07 | Does every catalog execution use only u3's budgeted `EngineClient` surface with a validated `BudgetSpec`, with no answer route through raw goals or `consult`? [P2.1,P2.8; inherited u3 P1.1,P6.2] Close: call-graph scan plus missing/invalid-budget service tests. | `.scratch/contract-m1u4.md` P2.1, P2.8; `.scratch/contract-m1u3.md` P1.1, P6.2 and D6 |
| c08 | Is every displayed result derived solely from the current live-run DTO, while committed answer fixtures remain absent from the production module and filesystem reachability graph? [P2.2] Close: source-reach scan plus a service test whose injected live DTO disagrees with the fixture. | `.scratch/contract-m1u4.md` P2.2; `.agent/roadmap.md` u4 accept |
| c09 | Does the service preserve distinct typed outcomes for stack, depth, inference, wall clock, answer cap, cancellation, heap, honest failure and each error, without collapsing any into “no answer”? [P2.3–P2.4] Close: exhaustive terminal-variant table plus zero-solution case. | `.scratch/contract-m1u4.md` P2.3–P2.4; `.scratch/contract-m1u3.md` P2.1–P2.9, P5.2 |
| c10 | Does an empty projection render existential yes/no rather than an empty binding list, with live `recommendation-exists` reporting yes from 12 proofs and an exhausted run reporting no? [P2.5] Close: real-PVM positive plus zero-proof negative. | `.scratch/contract-m1u4.md` P2.5 and Q Serializer/Service |
| c11 | Does the service discard undeclared bindings and avoid `JSON.stringify` on every engine value, preserving the guideline-compound and rational traps? [P2.6–P2.7] Close: extra-binding test, rational/guideline regression, and production scan. | `.scratch/contract-m1u4.md` P2.6–P2.7; `.scratch/contract-m1u2.md` P3.8–P3.12; `.agent/memory.md` Engine runtime |
| c12 | Is canonical ordering invariant under engine-yield permutations and implemented as SWI total term order, with no distinct solutions comparing equal? [P3.1–P3.2,D5] Close: mixed-shape permutation/property corpus where byte sort disagrees, plus a deliberately byte-sorting mutant that must fail. | `.scratch/contract-m1u4.md` D5, P3.1–P3.2, I standard-order; Q Serializer |
| c13 | Does category-A compare the live serializer bytes exactly with the `solutions([...])` substring extracted at test time from the verified bag's own answer file, never with a transcription or envelope? [P3.3,D6] Close: bag-read byte assertion plus one-byte expected/runtime mutants that must fail. | `.scratch/contract-m1u4.md` D6, P3.3, I byte claim; `.agent/memory.md` Oracle fidelity |
| c14 | Does each of the other three exported IDs receive its own named canonical value comparison against the bag answer, with exact missing/extra/divergent terms reported rather than a shared nonempty verdict? [P3.4] Close: three per-ID comparisons plus an injected divergence that names its ID and value. | `.scratch/contract-m1u4.md` P3.4 and I canonical value-equal; `.agent/polish.md` Four-query byte differential |
| c15 | Does each projected binding use engine `term_string/3` display text, then serialize only as `sol([values])` inside `solutions([...])`, with no JS term template? [P3.5–P3.6] Close: re-readable term-trap cases plus exact grammar assertions. | `.scratch/contract-m1u4.md` P3.5–P3.6; `.scratch/contract-m1u2.md` D1.3, P4.1–P4.3 |
| c16 | Is guideline-ID humanization structural over the five outer fields and generic across role, locator and opaque document ID, with no CDC atom, prefix, document title or token gloss in logic? [P4.1,D8] Close: full Q humanizer corpus plus forbidden-specificity source scan. | `.scratch/contract-m1u4.md` D8, P4.1 and Q Humanizer |
| c17 | Do malformed or unsupported IDs return their raw text without throwing, while every humanized label remains presentation-only and cannot affect goals, comparisons, sort keys or serialized bytes? [P4.2–P4.3] Close: malformed Q table plus label-taint invariance test. | `.scratch/contract-m1u4.md` P4.2–P4.3 and Q Humanizer |
| c18 | Does a real-PVM overlay probe first prove its unique clause absent, then declare the static predicate dynamic, inject it, and prove the displayed service result changes from the live baseline? [P5.1] Close: sentinel-presence test before/after injection and after fresh-engine cleanup. | `.scratch/contract-m1u4.md` P5.1 and I overlay; `.agent/memory.md` Generated runtime payload |
| c19 | Does `tools/kb/check.mjs` byte-scan every production root for `queries/answers` and fail on static import, dynamic `import()`, and filesystem read alike? [P5.2,D7] Close: committed negative controls that introduce each literal reach and observe `kb:asset-check` nonzero. | `.scratch/contract-m1u4.md` D7, P5.2 and I forbidden-reach; `tools/kb/check.mjs` |
| c20 | Are every test expectation and named claim independent of the artifact under test, with the two spike references remaining rerunnable and wrong-goal, hard-coded-result, byte-sort and transcribed-byte mutants turning green tests red? [P5.3,D9] Close: expectation-provenance audit, differential replay and mutation matrix. | `.scratch/contract-m1u4.md` D9, P5.3 and I; `CLAUDE.md` Engineering adversarial review |
| c21 | From committed state, does clean generation make `pnpm gate` rc 0, emit the catalog inside existing `kb:build`, reproduce byte-identically, and leave the sibling-path scan empty? [P6.1,P6.4,D1] Close: contract G commands 1, 4 and 5 with recorded rc/hash evidence. | `.scratch/contract-m1u4.md` P6.1, P6.4 and G; `package.json` scripts |
| c22 | Does each of all six catalog IDs have a committed live test against the real generated PVM, and do those tests fail rather than skip when `kb/generated/` is absent? [P6.2–P6.3] Close: per-ID test census, `pnpm test`, and absent-artifact negative control. | `.scratch/contract-m1u4.md` P6.2–P6.3 and G2; `.scratch/contract-m1u2.md` P5.5 |
| c23 | Does u4 leave `src/engine/*` byte-unchanged and add no main-thread `swipl-wasm` import, unbudgeted query export, undeclared runtime API, engine-value stringify, or literal runtime contract/count? [inherited u2/u3] Close: base-diff plus import/API/stringify/export/literal scans. | `.scratch/contract-m1u4.md` inherited-contract preface and I; `.scratch/contract-m1u2.md` P1.1, P3.12; `.scratch/contract-m1u3.md` P6.2–P6.5 |
| c24 | Does every touched durable file pass a per-file `CLAUDE.md` ledger: dense symbol-forward text, no provenance narrative, positive future instructions, ASD-STE100 copy, tight deduplicated modules, why-only comments, and deterministic checks for mechanical rules? Close: touched-file census, ledger adjudication, format/lint/check and negative controls. | `CLAUDE.md` Authoring and Engineering; `.scratch/contract-m1u4.md` tier `kernel` |

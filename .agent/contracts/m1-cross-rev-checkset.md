# rev-m1-cross — fixed check set for M1 cross-cutting review

24 rows. The milestone's LAST review surface: cross-unit integration plus
project-`CLAUDE.md` conformance, including instruction and memory
token-efficiency and obsolescence. Per-unit predicates are already adjudicated
(u1–u7, 320 rows) — this set owns what NO unit row owns: the seams between
units, the state files, and the repo-wide posture.

Sources = `.agent/roadmap.md`, `.agent/memory.md`, `.agent/polish.md`,
`.agent/review-m1.md`, `.agent/contracts/m1u{1..7}.md`, `CLAUDE.md`,
`.agent/initial-prompt.md`.

Review target = `0f08e10` (HEAD), whole tree.

Scope rule: a defect that a per-unit check set already adjudicated is OUT of
scope — cite the row id and move on. A defect that spans two units, or lives in
a state file, or is repo-wide, is IN scope even if a unit touched it.

Ids backticked so a grader reads one row per id from a report's verdict table alone.

| id | check | source | how to decide it |
| --- | --- | --- | --- |
| `X01` | mechanical · Does the module graph layer cleanly `kb → engine → questions → demo` with no import cycle and no upward import, and does `src/main.ts` reach the engine only through the declared seams? | `CLAUDE.md` Engineering (modular, deduplicate), u2/u4/u6 contracts | Build the import graph over `src/**`; report every cycle and every edge that points against the layering, with `file:line`. |
| `X02` | mechanical · Does the project's ONE non-negotiable hold end to end — every displayed answer traces to a genuine Prolog solution — with no committed fixture, no `queries/answers` byte, and no hard-coded answer string reachable from a production render path? | `.agent/roadmap.md` Goal, `.agent/initial-prompt.md`, u4 forbidden-reach scan | Trace every render site back to its data source; confirm the `kb:asset-check` byte scan's coverage set actually spans every production file; try to name one assembled-path evasion the scan misses. |
| `X03` | mechanical · Are budget, cancel and every terminal payload declared ONCE in `src/engine/protocol.ts` and imported by both sides, with every member surviving `structuredClone` — the per-union-member sweep, not one sample? | u3 P6.1, u2 P2.1/P2.4, `.agent/review-m1/rev-m1u3-4.md` R35 | Census duplicate type declarations across `src/engine/**` and `src/demo/**`; round-trip every member of every payload union through `structuredClone` and report which members had no committed coverage. |
| `X04` | mechanical · Is the question that DISPLAYS always the question that RUNS — catalog → serialize → humanize → service → controller → `AnswerPanel` — with the displayed string byte-derived from the same compiled goal the engine receives? | u4 D3-equivalent, u7 D3, `.agent/memory.md` catalog bullets | Follow one id through every hop and compare the displayed bytes against the goal's source bytes; confirm no hop re-writes, re-cases or re-wraps the string. |
| `X05` | mechanical · Is the error and outcome taxonomy CONSISTENT across u2/u3/u4/u6 — one exhaustive union, every member reachable, every member rendered distinctly, and no `default` catch-all standing in for `never`? | u3 R11 ruling, u6 V3/V4/V5, `CLAUDE.md` Engineering | Enumerate the union members at their declaration site; for each, find its production producer and its render branch; report members with no producer, no render branch, or a shared indistinct rendering. |
| `X06` | mechanical · Does the cancellation contract hold END TO END — `AbortSignal` → `AnswerService` → `EngineClient` → worker → Prolog — with exactly one `cancel` posted per abort, no orphan listener, and the terminal state owned by the query result rather than the cancel ack? | u6 D1/C3–C7, u3 P4.x, `.agent/contracts/m1u6.md` map S3/S4 | Drive an abort through the full stack with a spy at each hop; count posts and listeners at each; confirm the rendered terminal state is derived from the query result. |
| `X07` | mechanical · Is `DEMO_BUDGET` the ONLY budget any UI path passes, and is there no exported query surface that reaches the engine unbudgeted — the u3 P6.2 predicate, checked as a fact about the current tree rather than by construction? | u3 P6.2, u6 R9, `.agent/polish.md` "Forbidden-surface checks are unmechanized" | Census every exported function that can reach `EngineClient.query`; for each, decide whether a caller can omit a budget; name any that can. |
| `X08` | mechanical · Is `pnpm gate` ONE chain in which every step fails closed, and does every out-of-gate check (`kb:reproduce`, `smoke`, the visual-QA walker, `.scratch/verify-fixes.py`) have a recorded, accurate rerun path? | `.agent/memory.md` Gate, `CLAUDE.md` "gate backing a durable claim must rerun from committed state" | Read the chain for a step whose failure does not propagate; then take each out-of-gate check's recorded rerun command from memory and run it or prove it runnable; report every command that does not work as written. |
| `X09` | mechanical · Does the built output census reproduce — 14 files / 3773949 B raw, main chunk carrying 0 engine bytes, worker chunk plus hashed PVM separate — and is the recorded gzip figure either current or explicitly marked stale? | `.agent/memory.md` "Measured" build bullet, u2 built-output claim | `pnpm build` from clean; census file count and raw bytes; byte-scan the main chunk for engine identifiers; report every drift from the recorded line. |
| `X10` | mechanical · Do the two vitest projects (`node`, `dom`) together run EVERY committed test file, with no file excluded by both globs and no file silently skipped, and does the reported total match the file census? | u5 dom-project bullets, `.agent/memory.md` | Read both project globs; list `tests/**` and classify each file; run the suite and compare the reported file and test counts against the census. |
| `X11` | mechanical · Is `.gitignore` current — every generated artifact ignored, no generated artifact tracked, no tracked file stale — and does the trailing-slash convention memory records for `kb/generated` and `node_modules` still match the symlink reality? | `CLAUDE.md` "Keep `.gitignore` current", `.agent/memory.md` worktree bullets | `git status --porcelain --ignored` against a clean build; check each ignore rule matches something; verify the two no-trailing-slash rules against an actual symlinked worktree. |
| `X12` | mechanical · Is the read-exclusion SYNCED PAIR consistent — `.claude/settings.json` `permissions.deny` carrying the full set and `.serena/project.yml` `ignored_paths` carrying the committed non-gitignored subset — with every rule matching a real path and no required-readable path denied? | `CLAUDE.md` Claude Code read-exclusion rules, `.agent/memory.md` Read-exclusion set | Read both lists with `jq` and by ordinal line; verify entry counts against disk shape; Read-test one required-denied block and one required-readable path; check `git_ignore` handling of `.serena/cache/`. |
| `X13` | judgment · Is EVERY claim in `.agent/memory.md` still true at this commit, with no bullet obsoleted by a later unit or by a review fix, and does every recorded measurement carry the instrument it came from? | `.agent/memory.md`, `CLAUDE.md` Authoring | Walk the file bullet by bullet; for each falsifiable claim decide reproduce / stale / unverifiable-as-written; flag every Node figure presented without its browser counterpart and every number whose instrument is unnamed. |
| `X14` | judgment · Do `.agent/memory.md` and the project `CLAUDE.md` meet the Authoring rules — dense, symbol-forward, human-sparse, no provenance narrative, no dates, no discovery stories, no redundancy against code, docs or git history? | `CLAUDE.md` Authoring | Grade both files against each Authoring clause; quote the offending text with `file:line`; separately name content that duplicates what code or git already states and should be pruned. |
| `X15` | judgment · Does `.agent/roadmap.md` match reality — per-unit records agreeing with the ledger and the commits, every gauge recorded, every status current, the calibration arithmetic correct, and the review-session accounting adding up to the ledger's row totals? | `.agent/roadmap.md`, `.agent/review-m1.md` | Recompute the calibration ratios and the row sums from the ledger; compare every recorded unit status and gauge against its commit; report each mismatch with both figures. |
| `X16` | judgment · Does `.agent/polish.md` hold ONLY well-formed open entries — each with an acceptance check and a `pri`, none already closed by a later commit — and does every "NOT verified" item recorded in the roadmap have a matching entry? | `CLAUDE.md` polish-register rules, `.agent/roadmap.md` NOT-verified lines | Grade each entry for the two required fields; for each, check whether a later commit already closed it; cross-index the roadmap's NOT-verified lines and name any with no register entry. |
| `X17` | judgment · Does every `.agent/contracts/m1u*.md` verdict table still describe the SHIPPED surface — no contract asserting a predicate the code later lost, and no contract left stale by a review fix that changed the code it describes? | the seven contract files, `.agent/review-m1.md` | For each contract, sample the predicates a later commit could have broken and decide each; a contract whose text a review ruling superseded without an edit is a fail naming both. |
| `X18` | mechanical · Does every M1 commit follow `<scope> (<trace>): <cause> → <fix>`, is the trace key grep-able as the roadmap claims (`git log --grep "(M1[. ]"`), and does every commit subject state a real cause and a real fix rather than a summary? | `CLAUDE.md` Git, roadmap commit convention | Run the documented grep and compare its hit set against the full M1 log; parse each subject for the three parts; report every subject that omits one. |
| `X19` | judgment · Is there duplicated logic ACROSS units that should be one module — term rendering, id humanizing, state description, budget shaping, error formatting — and does the codebase hold to KISS and to the code-golf-within-three-bounds rule? | `CLAUDE.md` Engineering (deduplicate, refactor proactively, agent-legible) | Census near-duplicate functions and repeated literal sets across `src/**` and `tools/**`; for each candidate say whether merging serves the three bounds or violates them. |
| `X20` | judgment · Do comments across `src/**` and `tools/**` spend tokens on the WHY a fresh agent would re-derive, with no `what` restatement, no provenance, no dates and no discovery narrative? | `CLAUDE.md` Authoring comments rule | Sample every file's comments; classify each as why / what / provenance / noise; report the `what` and provenance comments with `file:line` and name any peculiar decision that carries NO why comment and needs one. |
| `X21` | judgment · Are the human-facing surfaces in ASD-STE100 register and the machine-consumed payloads left as code surface, with the boundary drawn correctly — and does the project ship the human-facing entry document a reader needs? | `CLAUDE.md` Authoring human-facing rule | Enumerate every human-facing surface (UI copy, licence page, any README or docs) and every machine payload (JSON fields, logs, error codes); grade each against the correct register; state plainly whether a shipped entry document exists. |
| `X22` | mechanical · Is the dependency posture intact — `swipl-wasm` exact-pinned per the R26 ruling, TypeScript held under the `typescript-eslint` <6.1 cap, `@types/cytoscape` absent, jsdom pinned at 29.1.1, no deprecated or unused package, and no dependency in the wrong section? | `.agent/memory.md` Install constraints + u3 R26 + u5 jsdom bullets | Read `package.json` and the lockfile for each pin form; census every declared dependency for an import site; run the package manager's own outdated and deprecation report and grade each finding. |
| `X23` | judgment · Can a fresh agent or user go from a clean clone to a running demo using ONLY committed instructions, and is every prerequisite the path needs actually stated? | `CLAUDE.md` Collaboration + Authoring, `.agent/roadmap.md` Goal | Walk the committed files for a setup path; execute it as written from a clean checkout state; report the first step that is missing, wrong, or assumed. |
| `X24` | judgment · Do the durable claims that rest on SCRATCH-LOCAL scripts carry their regeneration path in `.agent/memory.md`, and is the scheduled port recorded — the `CLAUDE.md` rule that a gate backing a durable claim must rerun from committed state? | `CLAUDE.md` Engineering gate rule, `.agent/memory.md` Scratch conventions, `.agent/polish.md` "Mutation harness is scratch-local" | Enumerate every durable claim whose evidence lives in `.scratch/` or on a `wt/` branch; for each, check memory records a working regeneration path and a scheduled port; run one recorded path and report whether it works as written. |

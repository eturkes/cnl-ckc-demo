# rev-m1u7 — fixed check set for M1.u7, demo presentation + honest framing

26 rows. Sources = `.agent/contracts/m1u7.md` (D1–D9, F/T/C/A/R/G predicates,
verdict table), `.agent/roadmap.md` u7, `.agent/memory.md` "Presentation and
framing (u7)", `.agent/polish.md` u7 entries, `CLAUDE.md`.

Tier `docs` ⇒ **spot-check grade**: sample the surface and grade claim
consistency. Every row still gets a verdict; a row whose full proof needs a real
browser reports what the sample showed and names the unmeasured remainder rather
than guessing.

Review target = `0f08e10` (HEAD). u7 shipped at `672f334`; the five M1-review
commits after it touched `src/engine`, `src/questions` and `tests`, so a u7
guarantee that a later review fix broke is a u7 row `fail`.

One recorded `fail(low)` already sits in the contract's verdict table — G4/G5.
Re-derive it: a recorded fail is where a second, unrecorded defect hides.

Ids backticked so a grader reads one row per id from a report's verdict table alone.

| id | check | source | how to decide it |
| --- | --- | --- | --- |
| `U7-01` | mechanical · Are all three `@fontsource-variable` packages pinned at exactly `5.3.0` in `dependencies`, and does no runtime code path fetch a font over the network — no `@import`, no absolute font URL, no Google Fonts host in `index.html`, `src/**` or `dist/**`? | F1, D1 | Read `package.json` `dependencies` for the pin form; byte-scan `index.html`, `src/`, and built `dist/` for a scheme-bearing font URL and for `fonts.googleapis`/`fonts.gstatic`. |
| `U7-02` | mechanical · Does a built `dist/` carry EXACTLY 6 `woff2` files totalling 176732 B and no other font file of any format? | F2, `.agent/memory.md` u7 font bullet | `pnpm build` from a clean `dist/`, then census every font extension (`woff2`, `woff`, `ttf`, `otf`, `eot`) under `dist/` and sum the bytes. |
| `U7-03` | mechanical · Do the three `@font-face` family names carry no `Variable` suffix, and do `--font-ui`, `--font-prose` and `--font-code` resolve to Atkinson Hyperlegible Next, Literata and Atkinson Hyperlegible Mono respectively, each retaining the previous system stack as fallback? | F3, F4 | Read every `@font-face` `font-family` and every `--font-*` declaration in `src/app.css`; confirm each token's value keeps a generic and system fallback tail. |
| `U7-04` | mechanical · Does EVERY `@font-face` rule set `font-display: swap`, and does each rule's `unicode-range` cover latin and latin-ext only? | F5, F2, D1 | Enumerate the `@font-face` blocks and assert both properties per block; a block missing either is a fail naming the family and weight. |
| `U7-05` | mechanical · Does `:root` define role-named tokens only, with no token naming an appearance, and is EVERY custom property consumed anywhere in `src/**` defined in `:root` — in particular is `--field`'s inline `#fff` fallback gone and `--field` defined? | T1, T2, D2 | Extract the `:root` declaration set and the `var(--…)` use set across `src/**`; diff both directions; grep for any surviving `var(--x, #…)` inline fallback. |
| `U7-06` | mechanical · Does `tools/contrast.mjs` parse `:root` out of `src/app.css`, compute WCAG relative luminance and contrast ratio correctly, and grade a DECLARED pair table rather than the DOM? | T3, D9 | Read the parser and the luminance maths against the WCAG 2.x formula; verify the flooring rule; confirm no DOM or canvas dependency. |
| `U7-07` | mechanical · Does every declared normal-size pair reach ≥4.5:1 and every large-text and non-text pair ≥3:1, with the thresholds applied per pair category rather than one global cutoff, and does `--border` measure ≥3:1 against `--surface`? | T4, `.agent/memory.md` `--border` `#8f8270` bullet | Run `pnpm contrast:check`; recompute at least three ratios independently, including `--border`/`--surface`; confirm the category-to-threshold mapping in the source. |
| `U7-08` | mechanical · Does the validator fail CLOSED on each of three degenerate inputs — a pair naming an undefined token, an unparseable colour, and an empty pair table — and does it also fail when a defined colour token appears in no pair? | T5, `.agent/memory.md` contrast bullet | Perturb a scratch copy three ways plus the orphan-token case; assert rc ≠ 0 and a message naming the cause each time; restore. |
| `U7-09` | mechanical · Negative control: does perturbing one token so a real pair drops below its threshold make `pnpm contrast:check` exit non-zero and NAME the failing pair, and does restoring the token return rc 0? | T6 | Reproduce the contract's recorded `--text-muted` perturbation; record rc, the named pairs, and the restore. |
| `U7-10` | mechanical · Is the h1 D7's wordmark VERBATIM, and does no user-visible string name a milestone, an unshipped feature or an internal identifier? | C1, D7 | Read the h1 string byte-for-byte against D7; census every user-visible string in `src/**` for `M1`–`M4`, unit ids, `TODO`, file names and catalog ids leaking into copy. |
| `U7-11` | mechanical · Does the lede describe what the demo does TODAY, with the M2 trace claim and the M3 graph claim absent from the shipped surface? | C2 | Read the lede and every panel string; grep the rendered copy for trace, provenance, graph and network claims; report any survivor with its site. |
| `U7-12` | mechanical · Does all u7 prose live in `src/demo/copy.ts` as `INSTRUCTIONS` and `DESCRIPTIONS`, and does `tools/copy-check.mjs` grade EVERY string in both records at ≤20 and ≤25 words per sentence and also grade `src/demo/describe.ts`? | C3, C4, D8 | Census literal user-visible strings in `src/App.svelte`, `src/demo/*.svelte` and `src/questions/*.svelte` that are not in `copy.ts`; read the validator's file list and its per-record limits; confirm the sentence splitter treats `1.1.` as one sentence. |
| `U7-13` | mechanical · Is the banned-filler list (`simply`, `robust`, `seamlessly`, `leverage`) actually enforced by the validator, and does a `${…}` interpolation count as exactly one word? | C5 | Inject each banned word into a scratch copy and assert rc ≠ 0; inject a sentence whose word count crosses the limit only if an interpolation counts as more than one word and assert it passes. |
| `U7-14` | mechanical · Negative control: does a 30-word sentence added to either record make `pnpm copy:check` exit non-zero and name the FILE, the KEY and the word COUNT? | C6 | Add the sentence to each record in turn; record rc and the three named fields; restore and confirm rc 0. |
| `U7-15` | mechanical · Do descriptor labels render through the D4 formatter so no `<dt>` shows `noun(...)` or `wh(...)`, does the formatter fall back to the raw descriptor for an unrecognized shape, and do the six displayed question strings stay VERBATIM from the catalog and ungraded by the copy validator? | C7, D3, D4 | Render all six questions' settled answers and read every `<dt>`; feed the formatter an unrecognized descriptor and assert the raw fallback; compare each displayed question string against the generated catalog bytes. |
| `U7-16` | mechanical · Does the footer carry all three CDC requirements — attribution naming the developing agency, a nonendorsement disclaimer, and a free-availability statement — ALWAYS VISIBLE and never inside a `<details>`? | A1, A2, A3, D5 | Read the footer markup and assert none of the three sits inside a disclosure; confirm each is present at first render with the disclosure closed. |
| `U7-17` | mechanical · Does the About disclosure state all four honest-framing facts — that the demo runs a compiled projection rather than reproducing CDC text unchanged, that all 337 documents carry the bag's `unreviewed` label within its five-value vocabulary, that the question list is fixed, and that the demo is not for clinical use? | A4, A5, A6 | Read the About panel strings; check each of the four claims is present and stated as a fact; verify the `unreviewed` vocabulary matches upstream `tools/goal.py:3140`'s five values. |
| `U7-18` | mechanical · Does `dist/licenses/` carry each font family's OFL text BYTE-EQUAL to that package's own `LICENSE`, and does the disclosure link all three? | A7 | `cmp` each shipped licence against `node_modules/@fontsource-variable/*/LICENSE`; read the three link hrefs out of the About panel and confirm each resolves in `dist/`. |
| `U7-19` | mechanical · Is there no horizontal overflow — `documentElement.scrollWidth <= clientWidth` — at 320, 375 and 1280 CSS px in every reachable state? | R1, G4, G5 | Rerun the recorded walker evidence or state plainly that it was not rerun; if not rerun, grade the recorded measurement's provenance and say what remains unmeasured at this commit. |
| `U7-20` | mechanical · Does the answer composition ADAPT rather than merely wrap — one column narrow, two columns where width allows — does page padding scale with viewport so the fixed `4rem 1.5rem` does not survive at 320 px, and does every text surface that can receive a long engine-authored string keep `overflow-wrap`? | R2, R3, R4 | Read the `dl` grid rules and the breakpoint; read the padding declaration for a `clamp` or query; census every element that renders a binding, a question string or an error message for `overflow-wrap`. |
| `U7-21` | mechanical · Is `pnpm gate` rc 0 from a clean `kb/generated`, does the chain match the contract's declared gate identity in order, and does `pnpm check` report 0 errors and 0 warnings under `--fail-on-warnings`? | G1, G2, gate identity | `rm -rf kb/generated && pnpm gate`; record rc, file count, error and warning counts, test count and asset count; compare the script's step order against the contract string. |
| `U7-22` | mechanical · Does `pnpm smoke` still exit 0, with the real browser answering `dosage-reduction-content` and the rendered canonical text equal to the bag's committed bytes read at run time, and does it open the D6 disclosure before reading it? | G3, D6, `.agent/memory.md` smoke bullets | Run `pnpm smoke`; read the disclosure-opening step in `tools/smoke.mjs`; confirm the comparison source is `verifyBag` at run time rather than a fixture. |
| `U7-23` | judgment · Is the recorded G4/G5 `fail(low)` ruling sound and complete — the walker committed only on `wt/map-m1u7` `124e34d`, its `svelte-check` error count as recorded, `pnpm visual-qa` absent from `package.json`, and the polish entry well-formed with an acceptance check and a `pri`? | G4, G5, contract verdict table, `.agent/polish.md` "Typed port of the visual-QA walker" | Confirm the walker is absent from the primary tree and from `package.json`; check out the branch and count its `svelte-check` errors; read the polish entry against the deferral-time contract. Report any part of the recorded ruling that does not reproduce. |
| `U7-24` | judgment · Do u7's recorded NUMBERS reproduce at this commit — 6 woff2 / 176732 B, 79 copy strings, 17 contrast pairs, 355 files 0 errors 0 warnings, 171 tests, 3 assets, `dist/` 14 files / 3773949 B, 548-line probe, 64 vs 41 `svelte-check` errors on the walker? | `.agent/roadmap.md` u7, `.agent/memory.md` u7, contract verdict table | Rerun each measurement or mark it unrerun; flag every mismatch including the contract-vs-roadmap disagreement on the walker's error count. A number that moved because a later commit changed the tree is a stale-claim fail, not a code fail. |
| `U7-25` | judgment · Does each of D1–D9 still hold against the shipped surface, and is each rejected alternative's stated reason accurate rather than merely plausible? | D1–D9 | Walk each decision to its use site; for D1's rejected vendored-`woff2` route, D5's CDC-prominence rationale and D9's jsdom-canvas rationale, verify the stated blocking reason is real and still true. |
| `U7-26` | judgment · Does every durable file u7 touched pass a per-file `CLAUDE.md` ledger — Authoring (dense symbol-forward internal text, no provenance narrative, ASD-STE100 register for human-facing copy) and Engineering (deterministic checks own every tool-decidable rule, why-only comments, deduplicated modules, KISS)? | `CLAUDE.md` Authoring + Engineering, D8 | Adjudicate `src/app.css`, `src/demo/{copy,descriptor}.ts`, `src/demo/AboutPanel.svelte`, `src/App.svelte`, `src/demo/AnswerPanel.svelte`, `tools/copy-check.mjs`, `tools/contrast.mjs` file by file; name any tool-decidable u7 rule that no gate step decides. |

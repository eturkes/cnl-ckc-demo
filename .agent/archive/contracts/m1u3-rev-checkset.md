# M1.u3 adversarial check set

Fixed before implementation-diff inspection. Baseline grounding = `2f78353`; adjudication follows in phase 2.
Coupled obligations share rows; every M1.u3 predicate appears at least once.

| id | check | source | how to decide it |
| --- | --- | --- | --- |
| R01 | Every public query requires stack, depth, inference, and wall-clock budgets. No overload, default, raw `solve`, or alternate export lets u4 or the UI omit them. | P1.1; P6.2; roadmap M1.u3 Accept: “no unbounded query reaches the UI” | Mechanical — inspect every export and call path with `/usr/bin/rg -n 'query\(' src/engine` and `/usr/bin/rg -n 'solve\(' src/engine`; run `pnpm check`; a compile-negative caller without a budget must fail. |
| R02 | Client and worker independently reject every missing, zero, negative, fractional, non-finite, unsafe, and above-maximum budget value before any Prolog call. Maximum values remain accepted. | P1.2; P1.3; Q budget cases | Mechanical — run the P1.2/P1.3 matrix through the public client and directly through worker dispatch; a spy engine must record zero calls for invalid cases; include `NaN`, `Infinity`, `2^53`, each maximum, and maximum plus one. |
| R03 | The JS driver enforces `answerCap`; cap exhaustion is terminal and distinct from honest exhaustion. Category-A at cap 1 returns one answer then the cap state; `repeat` cannot build an unbounded list. | P1.4; P2.5; P3.5; D2; Q budget cases | Mechanical — run committed real-PVM cases for category-A with cap 1, normal seven-answer exhaustion, and `repeat` under a full budget; assert exact counts and distinct terminal discriminants. |
| R04 | Stack exhaustion has its own terminal state. The wrapper lowers the prior stack flag and restores that exact prior value after success, failure, stack trip, other limit, exception, and cancel. | P2.1; P3.1 | Mechanical — against the real PVM, read `stack_limit`, trip a low stack guard, exercise every exit path, read the flag after each, and assert the stack state is neither a generic Prolog error nor another limit. |
| R05 | Depth exhaustion has its own terminal state and composes with backtracking: early solutions remain visible before the terminal depth outcome. | P2.2; P3.2 | Mechanical — run a real-engine branchy goal with shallow answers followed by depth exhaustion; assert answer order, exact `depth` terminal kind, and distinction from inference, stack, failure, and Prolog error. |
| R06 | Inference exhaustion has its own terminal state and composes with backtracking: early solutions remain visible before the terminal inference outcome. | P2.3; P3.2 | Mechanical — run a real-engine branchy goal with shallow answers followed by inference exhaustion; assert answer order, exact `inference` terminal kind, and distinction from depth, stack, failure, and Prolog error. |
| R07 | Wall-clock covers dispatch through display rendering. A soft worker check between solutions preserves the engine; a hard main-thread watchdog terminates and recreates when one step blocks. | P2.4; P3.3; P3.6 | Mechanical — use an injected clock for dispatch-to-terminal accounting, a real-PVM multi-solution case for soft expiry, and a hostile single-step goal for hard expiry; assert `wall-clock`, engine reuse only after soft expiry, and replacement only after hard expiry. |
| R08 | Cancellation has its own terminal state. A queued cancel is received and acted on during a multi-solution query through a worker macrotask yield, including before the first solution and between solutions. | P2.6; P4.1; D4; Q cancel cases | Mechanical — drive the shipped worker with real-PVM multi-solution goals; cancel before solution one and between later solutions; assert mid-run receipt, typed `cancelled`, bounded settlement, and no limit/error alias. |
| R09 | Heap exhaustion has its own terminal state and forces engine recreation rather than reuse of saturated heap or asserted residue. | P2.7; D9 | Mechanical — induce bounded real-engine heap exhaustion in an isolated worker; assert the exact heap terminal kind, one terminate-and-recreate cycle, fresh contract verification, and absence of pre-trip dynamic residue. |
| R10 | Limit classification matches decoded `PlTerm` structure only, and u3 leaves `src/engine/terms.ts` byte-unchanged. Message text cannot select a limit. | P2.8; P6.5; D7 | Mechanical — run `git diff --exit-code 2f78353 -- src/engine/terms.ts`; inspect the classifier for exact atom/functor/argument checks; feed identical messages with different decoded structures and assert only structure controls classification. |
| R11 | `isTerminal` and every client-side outcome switch are compiler-exhaustive over all terminal variants. Adding a variant without handling it must fail compilation. | P2.9 | Mechanical — inspect for a `never`-checked exhaustiveness seam; in a disposable copy add one terminal protocol variant, run `pnpm check`, require failure at `isTerminal` and every client switch, then handle it and require success. |
| R12 | Every budget trip leaves the logical session sound. Soft trips retain the engine; hard wall and heap trips recreate it. Each path still reads schema 1, 337 documents, and seven category-A solutions from the live engine. | P3.4; Q budget cases | Mechanical — trip stack, depth, inference, answer cap, soft wall, hard wall, and heap independently; also set all limits low and test depth/inference precedence; after each, query contract and category-A through the same client session and assert the expected generation behavior. |
| R13 | Cooperative cancel targets one in-flight request ID, settles no other request, preserves the worker generation, and leaves the engine usable without reboot. Already-settled, unknown, and duplicate cancels close deterministically. | P4.2; P4.3; Q cancel cases | Mechanical — run two correlated real-PVM requests, cancel one, and issue settled/unknown/duplicate cancels; assert only the target cancels, boot count and generation stay fixed, and a following category-A query succeeds. |
| R14 | Hard cancel terminates the worker, creates a new worker/session/engine, refetches and reloads the PVM, and resolves readiness only after the replacement re-reads and verifies the manifest contract. | P4.4; D5 | Mechanical — instrument the worker factory, fetch/load seams, and boot responses; trigger hard cancel; require one old-worker termination, one fresh construction and load, and no ready result before replacement contract verification succeeds. |
| R15 | Hard cancel drops asserted state. The overlay is observably present before cancellation, absent afterward, and the replacement reports its 337-document contract from engine output rather than a literal. | P4.5; I: “Hard-cancel recovery is proven by an overlay that was observably present beforehand”; roadmap M1.u3 Accept | Mechanical — declare `dynamic/1`, assert a PID-unique overlay, query it successfully, hard-cancel, then assert overlay absence and engine-reported document count 337 after replacement boot. |
| R16 | Every request in flight at hard cancel settles exactly once with its correct typed cause. The pending map empties, and no unhandled rejection occurs. | P4.6; inherited u2 P2.3 and P2.5 | Mechanical — hold at least two fake-worker responses, trigger hard cancel, count each resolver invocation, inspect pending size, and install an `unhandledRejection` trap; repeat with worker `error` and `messageerror`. |
| R17 | A request issued during respawn settles with a typed restarting/lifecycle outcome. It neither hangs nor attaches to the terminated worker. | P4.7; Q cancel cases | Mechanical — pause replacement boot with an injected worker factory, issue a query in that window, and assert bounded typed settlement, zero posts to the dead worker, and normal operation after boot completes. |
| R18 | Request IDs remain monotonic across worker generations. A stale response or watchdog from a dead generation cannot settle, cancel, or terminate a live-generation request. | P4.8; Q cancel cases | Mechanical — retain handles to old fake workers and timers, respawn, issue a new request, deliver stale response/timer events, and assert increasing IDs plus no settlement or termination of the new request. |
| R19 | Hard cancel is single-flight across concurrent user cancels, watchdog expiry, worker failure, and repeated reset calls. One generation produces at most one terminate and one respawn. | P4.9 | Mechanical — fire concurrent hard-stop triggers under an injected worker factory and clock; assert one `terminate`, one replacement factory call, one boot, and a shared lifecycle result for all awaiters. |
| R20 | Every watchdog timer clears on every terminal outcome: solutions, failure, each limit, cancellation, protocol/worker error, post failure, hard reset, and dispose. | P4.10 | Mechanical — use fake timers and expose timer counts; drive every terminal branch and require zero armed request timers afterward; advance the clock and assert no late termination or duplicate settlement. |
| R21 | `dispose()` is terminal and never respawns. Hard cancel is a separate awaitable lifecycle operation. Neither path calls `_PL_halt`. | P4.11; Q cancel cases | Mechanical — dispose during an active query, then advance timers and emit worker events; assert one termination, typed one-time settlement, zero factory calls afterward, and `/usr/bin/rg -n '_PL_halt' src` returns no production match. |
| R22 | A malformed goal fails through the existing parse guard as a typed Prolog error and executes no prefix. An honest zero-solution goal remains `failure`, distinct from every limit and malformed input. | P5.1; P5.2; roadmap M1.u3 Accept | Mechanical — run both against the real PVM; use a malformed goal containing an incomplete side-effecting prefix, then probe that no clause was asserted; assert separate `prolog` and `failure` outcomes. |
| R23 | Runtime consult captures stderr through both `printErr` and `on_output`, drains diagnostics per phase, and treats every `ERROR` line as fatal without trusting consult success. Clean consults pass; the two qsave `library(shlib)` warnings remain tolerated only at image load. | P5.3; D6; Q consult cases | Mechanical — exercise clean, syntax-error, failing-directive, `printErr`-only, and `on_output`-only consults against the real runtime; assert drain isolation and fatality; load the saved image with each tolerated warning and reject any extra warning/error. |
| R24 | Any consult that emits `ERROR` contaminates and discards its engine. Clauses loaded before or after the diagnostic are never reachable through a usable returned session. | P5.4; D6 | Mechanical — consult text that defines probes around a syntax error and failing directive, prove the old engine contains them only inside the harness, then require worker replacement and probe absence through the client’s next usable session. |
| R25 | Budget, cancel, lifecycle, and every terminal payload are plain structured-clone-safe DTOs whose shapes are declared once in `protocol.ts` and imported by both sides. | P6.1 | Mechanical — construct representative payloads for every union member and require `structuredClone(x)` deep-equality; inspect worker/client imports and run `/usr/bin/rg -n 'BudgetSpec' src` and `/usr/bin/rg -n 'EngineResponse' src`; require one declaration site per shape. |
| R26 | The dedicated worker remains the sole `swipl-wasm` owner, and production calls only declared package API. Main-thread code carries neither engine imports nor state; no `consult`, `abort`, heartbeat, or other undeclared runtime method is called. | P6.3; P6.4; D8; inherited u2 P1.1 | Mechanical — run source/import-graph and built-chunk scans for `swipl-wasm`; compare every production engine method call with `node_modules/swipl-wasm/**/*.d.ts`; fail any undeclared call or main-chunk engine byte. |
| R27 | The exact clean-state gate regenerates the PVM and passes from committed state. | P7.1; G1 | Mechanical — from the committed review target run `rm -rf kb/generated && pnpm gate`; pass only on rc 0, recording the commit SHA and full test/build summary. |
| R28 | Every limit, cancel, and consult predicate has a committed real-PVM test. `pnpm test` is green with generated assets and fails rather than skips when `kb/generated` is absent. | P7.2; P7.3; inherited u2 P5.5; G2 | Mechanical — run `pnpm test`; inspect each named predicate test for actual `kb.pvm` loading and no stub; in a disposable checkout remove `kb/generated`, rerun `pnpm test`, and require nonzero rc caused by missing assets. |
| R29 | Client lifecycle tests use injected worker-factory and clock seams to drive worker `error`, `messageerror`, timeout/cancel races, respawn, stale events, timer cleanup, and dispose. | P7.4 | Mechanical — inspect the test seam and event sources, then run `pnpm test`; require direct assertions for every named lifecycle event rather than indirect coverage through a happy-path worker mock. |
| R30 | Schema and document pass conditions come independently from engine output and manifest data, never production literals or constants computed from the artifact under test. | I: “337 and schema 1 stay values read from the engine and the manifest, never literal pass conditions”; I: “No test asserts a constant it computed from the artifact under test” | Mechanical — run `/usr/bin/rg -n '\b337\b' src tests` and `/usr/bin/rg -n 'schemaVersion: 1' src tests`; trace every hit’s provenance; perturb an independent expected contract and require the check to fail. |
| R31 | Each budget test proves that the intended live limit fired. A stubbed or early-returning engine cannot satisfy the assertion. | I: “A budget-limit test proves the limit fired, not merely that the call returned” | Mechanical — for each limit prove a control budget succeeds, the lower budget reaches the exact trip state, and an engine-side counter/readback changes; replace the engine with a returning stub and require the test to fail. |
| R32 | Stack restoration is observed from the engine after a trip and compared with the value read before it; no literal restoration target is assumed. | I: “The stack flag’s restored value is read back from the engine after the trip, not assumed” | Mechanical — read the initial stack flag, lower and trip it, read it again through the same engine, and assert equality with the first read; inspect the test for no hard-coded 1 GiB expectation. |
| R33 | The full inherited u2 contract remains green: worker ownership/boot, DTO correlation, term decode/encode/display, live contract, hygiene, main-thread engine exclusion, and sibling-path exclusion. | inherited u2 P1.1–P1.5, P2.1–P2.5, P3.1–P3.12, P4.1–P4.3, P5.1–P5.5, P6.1–P6.4; G3; G4 | Mechanical — run the complete inherited suite and forbidden-surface checker plus `pnpm kb:asset-check`; run `pnpm build`, inspect emitted main chunks for engine signatures, and require every u2 predicate test and scan to remain green. |
| R34 | Every roadmap acceptance clause has direct evidence: distinct typed limits; overlay-dropping hard cancel with 337 readiness; malformed goal and consult `ERROR` fail-closed behavior; bounded `repeat`. | roadmap M1.u3 Accept | Judgment — map each clause to one direct committed test or browser probe and its observed assertion; fail a clause backed only by type shape, source inspection, another clause, or the aggregate gate result. |
| R35 | Kernel assurance is a full adversarial battery. Deterministic validators own decidable invariants, and the Q corpus covers isolated/combined maxima, precedence, cancel races, stale generations, active dispose, diagnostic routing, and tolerated warnings. | CLAUDE.md Engineering: “Deterministic checks own every rule a tool can decide”; “kernel … = full adversarial battery”; “Target sufficient scope, evidence-backed claims, and real success criteria” | Judgment — inspect the predicate-to-test matrix and Q-corpus coverage before reading outcomes; require deterministic checks where possible and concrete evidence for every omitted or sampled edge. |
| R36 | Every touched durable file remains tightly scoped, agent-legible, deduplicated, and MVP-spine-only. Comments explain constraints rather than mechanics; human-facing additions obey the project register; any new cache/generated path updates the required ignore controls. | CLAUDE.md Authoring: agent-optimized artifacts and ASD-STE100 human-facing text; Engineering: KISS, why-only comments, MVP spine; Claude Code: read-exclusion synchronization | Judgment — inspect `git diff --name-only 2f78353..HEAD` and each diff; reject off-spine features, redundant protocol shapes, unexplained peculiarities, prose-rule breaches, or unsynchronized `.gitignore`/read-exclusion changes. |
| R37 | D1 hybrid-split claim audit. Falsifier = shipped composition loses a distinct Prolog outcome, fails stack restoration, or lacks a working JS hard stop. Existing evidence covers Node-hosted real-PVM Prolog composition and Node-worker watchdog behavior, not the complete browser-shipped composition. | D1; guarantee-versus-claim lens | Judgment — compare shipped wrapper order and lifecycle to both spike probes; rerun distinct-limit and restoration cases on the real PVM; require browser evidence for the JS hard-stop portion before treating D1 as a browser guarantee. |
| R38 | D2 Prolog-insufficiency claim audit. Falsifier = a full-budget `repeat` run escapes both answer cap and deadline, or accumulates an unbounded response. Existing evidence proves the PL wrapper alone emitted 100000 answers in Node; it does not prove shipped JS controls. | D2; guarantee-versus-claim lens | Mechanical — run `repeat` through the shipped worker in a real browser with both cap and deadline; require bounded memory/result size and one exact cap or wall terminal state within the configured bound. |
| R39 | D3 main-watchdog claim audit. Falsifier = the shipped browser main-thread timer is starved by worker WASM, or a worker-local timer supplies the hard bound. Existing evidence is Node `worker_threads` plus a Node-hosted worker timer, not browser Worker scheduling. | D3; guarantee-versus-claim lens | Mechanical — against built output in a real browser, start a synchronous hostile goal, record main and worker timer timestamps, and require the main timer plus termination to occur while the worker timer remains blocked. |
| R40 | D4 cooperative-granularity claim audit. Falsifier = the chosen macrotask does not admit a cancel message between answers, or a reachable goal has a single `next()` beyond the stated bound. Existing evidence covers a flag flip and 80 real steps, not browser message delivery or all u4 goals; D4 cites 62.00 ms while the current JS spike row records 50.11 ms. | D4; guarantee-versus-claim lens | Judgment — inspect that the yield is a true worker macrotask, prove browser cancel delivery between solutions, benchmark every reachable compiled goal, and constrain any latency claim to the measured corpus and maximum. |
| R41 | D5 terminate-and-recreate claim audit. Falsifier = browser termination leaves the worker/WASM consuming CPU, replacement reuses residue, contract verification fails, or restart latency contradicts UI behavior. Node evidence only: D5 cites 3.52 ms termination, while the current JS spike row records 5.72 ms; recreation spans 181.75–261.58 ms, and browser evidence is only a 335 ms boot floor. | D5; guarantee-versus-claim lens | Mechanical — require a committed built-output browser probe over at least five hostile-loop cycles; CDP must observe old worker-target removal, stopped CPU work, fresh overlay-free state, contract readiness, and measured termination/restart distributions. |
| R42 | D6 consult-fatality claim audit. Falsifier = either diagnostic hook loses an `ERROR`, clean consult is rejected, or the contaminated engine becomes usable. Existing evidence uses the same real PVM and swipl-wasm in Node, but not shipped browser loader/output wiring. | D6; guarantee-versus-claim lens | Mechanical — run clean, syntax-error, and failing-directive consults through the built browser worker with each hook isolated; require fatal typed settlement, worker disposal, fresh boot, and absence of every probe clause. |
| R43 | D7 structural-classification claim audit. Falsifier = any limit branch reads exception/message text, or stack reaches the native message-only path. Existing evidence covers decoded-term behavior and wrapper probes, not the shipped classifier. | D7; guarantee-versus-claim lens | Mechanical — keep `terms.ts` unchanged, inspect every classifier branch, inject misleading/changed messages around identical structures, and require exact atom/compound structure alone to select stack, depth, and inference states. |
| R44 | D8 declared-API and iterator-scope claim audit. Falsifier = production calls any undeclared `swipl-wasm` method, or solution-boundary yielding cannot meet the claimed reachable-goal granularity. Package-declaration evidence covers version 8.0.7 exactly; the 62 ms bound covers only 80 sampled Node steps. | D8; guarantee-versus-claim lens | Judgment — diff every production method call against the installed `.d.ts`, prove a real macrotask between iterator steps, benchmark all reachable goals, and reject any universal latency claim beyond that measured set. |
| R45 | D9 heap-state claim audit. Falsifier = browser exhaustion kills the worker/tab or reports a different failure, typed heap state does not force recreation, or saturated/residual state survives. Existing 2222464 KiB and 19.26 s evidence is a Node-process measurement, not browser memory behavior or reclamation. | D9; guarantee-versus-claim lens | Mechanical — run a bounded isolated-browser memory probe under an external watchdog; accept either decoded heap exhaustion or typed worker death only if the client recreates, re-verifies contract, drops residue, and reports the precise observed cause without message-text classification. |

Rows = 45. Mechanically decidable = 39. Judgment rows = R34, R35, R36, R37, R40, R44.

## Register

No phase-1 observations. During adjudication, add only out-of-contract items with an evidence pointer and a concrete acceptance check.

